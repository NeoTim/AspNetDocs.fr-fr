---
uid: signalr/overview/older-versions/troubleshooting
title: Dépannage SignalR (SignalR 1.x) Microsoft Docs
author: bradygaster
description: Cet article décrit les problèmes communs avec le développement d’applications SignalR.
ms.author: bradyg
ms.date: 06/05/2013
ms.assetid: 347210ba-c452-4feb-886f-b51d89f58971
msc.legacyurl: /signalr/overview/older-versions/troubleshooting
msc.type: authoredcontent
ms.openlocfilehash: e65ce086d28cff2a36c609f47a05af632081be63
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 04/17/2020
ms.locfileid: "81543091"
---
# <a name="signalr-troubleshooting-signalr-1x"></a><span data-ttu-id="a0cea-103">Résolution des problèmes de SignalR (SignalR 1.x)</span><span class="sxs-lookup"><span data-stu-id="a0cea-103">SignalR Troubleshooting (SignalR 1.x)</span></span>

<span data-ttu-id="a0cea-104">par [Patrick Fletcher](https://github.com/pfletcher)</span><span class="sxs-lookup"><span data-stu-id="a0cea-104">by [Patrick Fletcher](https://github.com/pfletcher)</span></span>

[!INCLUDE [Consider ASP.NET Core SignalR](~/includes/signalr/signalr-version-disambiguation.md)]

> <span data-ttu-id="a0cea-105">Ce document décrit les problèmes courants de dépannage avec SignalR.</span><span class="sxs-lookup"><span data-stu-id="a0cea-105">This document describes common troubleshooting issues with SignalR.</span></span>

<span data-ttu-id="a0cea-106">Ce document contient les sections suivantes.</span><span class="sxs-lookup"><span data-stu-id="a0cea-106">This document contains the following sections.</span></span>

- [<span data-ttu-id="a0cea-107">Les méthodes d’appel entre le client et le serveur échouent silencieusement</span><span class="sxs-lookup"><span data-stu-id="a0cea-107">Calling methods between the client and server silently fails</span></span>](#connection)
- [<span data-ttu-id="a0cea-108">Autres problèmes de connexion</span><span class="sxs-lookup"><span data-stu-id="a0cea-108">Other connection issues</span></span>](#other)
- [<span data-ttu-id="a0cea-109">Compilation et erreurs côté serveur</span><span class="sxs-lookup"><span data-stu-id="a0cea-109">Compilation and server-side errors</span></span>](#server)
- [<span data-ttu-id="a0cea-110">Problèmes de studio visuel</span><span class="sxs-lookup"><span data-stu-id="a0cea-110">Visual Studio issues</span></span>](#vs)
- [<span data-ttu-id="a0cea-111">Questions relatives aux services d’information sur Internet</span><span class="sxs-lookup"><span data-stu-id="a0cea-111">Internet Information Services issues</span></span>](#iis)
- [<span data-ttu-id="a0cea-112">Questions Azure</span><span class="sxs-lookup"><span data-stu-id="a0cea-112">Azure issues</span></span>](#azure)

<a id="connection"></a>

## <a name="calling-methods-between-the-client-and-server-silently-fails"></a><span data-ttu-id="a0cea-113">Les méthodes d’appel entre le client et le serveur échouent silencieusement</span><span class="sxs-lookup"><span data-stu-id="a0cea-113">Calling methods between the client and server silently fails</span></span>

<span data-ttu-id="a0cea-114">Cette section décrit les causes possibles d’un appel de méthode entre le client et le serveur pour échouer sans un message d’erreur significatif.</span><span class="sxs-lookup"><span data-stu-id="a0cea-114">This section describes possible causes for a method call between client and server to fail without a meaningful error message.</span></span> <span data-ttu-id="a0cea-115">Dans une application SignalR, le serveur n’a aucune information sur les méthodes que le client implémente; lorsque le serveur invoque une méthode client, le nom de la méthode et les données de paramètres sont envoyés au client, et la méthode n’est exécutée que si elle existe dans le format spécifié par le serveur.</span><span class="sxs-lookup"><span data-stu-id="a0cea-115">In a SignalR application, the server has no information about the methods that the client implements; when the server invokes a client method, the method name and parameter data are sent to the client, and the method is executed only if it exists in the format that the server specified.</span></span> <span data-ttu-id="a0cea-116">Si aucune méthode de correspondance n’est trouvée sur le client, rien ne se passe, et aucun message d’erreur n’est soulevé sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="a0cea-116">If no matching method is found on the client, nothing happens, and no error message is raised on the server.</span></span>

<span data-ttu-id="a0cea-117">Pour enquêter davantage sur les méthodes des clients ne se faisant pas appeler, vous pouvez activer la connexion avant d’appeler la méthode de démarrage sur le hub pour voir quels appels proviennent du serveur.</span><span class="sxs-lookup"><span data-stu-id="a0cea-117">To further investigate client methods not getting called, you can turn on logging before calling the start method on the hub to see what calls are coming from the server.</span></span> <span data-ttu-id="a0cea-118">Pour activer la connexion à une application JavaScript, découvrez [comment activer la connexion client (version client JavaScript)](../guide-to-the-api/hubs-api-guide-javascript-client.md#logging).</span><span class="sxs-lookup"><span data-stu-id="a0cea-118">To enable logging in a JavaScript application, see [How to enable client-side logging (JavaScript client version)](../guide-to-the-api/hubs-api-guide-javascript-client.md#logging).</span></span> <span data-ttu-id="a0cea-119">Pour activer la connexion dans une application client .NET, voir [Comment activer l’enregistrement côté client (.NET Client version)](../guide-to-the-api/hubs-api-guide-net-client.md#logging).</span><span class="sxs-lookup"><span data-stu-id="a0cea-119">To enable logging in a .NET client application, see [How to enable client-side logging (.NET Client version)](../guide-to-the-api/hubs-api-guide-net-client.md#logging).</span></span>

### <a name="misspelled-method-incorrect-method-signature-or-incorrect-hub-name"></a><span data-ttu-id="a0cea-120">Méthode mal orthographiée, signature incorrecte de la méthode ou nom incorrect du moyeu</span><span class="sxs-lookup"><span data-stu-id="a0cea-120">Misspelled method, incorrect method signature, or incorrect hub name</span></span>

<span data-ttu-id="a0cea-121">Si le nom ou la signature d’une méthode appelée ne correspond pas exactement à une méthode appropriée sur le client, l’appel échouera.</span><span class="sxs-lookup"><span data-stu-id="a0cea-121">If the name or signature of a called method does not exactly match an appropriate method on the client, the call will fail.</span></span> <span data-ttu-id="a0cea-122">Vérifiez que le nom de méthode appelé par le serveur correspond au nom de la méthode sur le client.</span><span class="sxs-lookup"><span data-stu-id="a0cea-122">Verify that the method name called by the server matches the name of the method on the client.</span></span> <span data-ttu-id="a0cea-123">En outre, SignalR crée le proxy hub en utilisant des méthodes à `SendMessage` dos de chameau, `sendMessage` comme il est approprié dans JavaScript, de sorte qu’une méthode appelée sur le serveur serait appelé dans le proxy client.</span><span class="sxs-lookup"><span data-stu-id="a0cea-123">Also, SignalR creates the hub proxy using camel-cased methods, as is appropriate in JavaScript, so a method called `SendMessage` on the server would be called `sendMessage` in the client proxy.</span></span> <span data-ttu-id="a0cea-124">Si vous `HubName` utilisez l’attribut dans votre code côté serveur, vérifiez que le nom utilisé correspond au nom utilisé pour créer le moyeu sur le client.</span><span class="sxs-lookup"><span data-stu-id="a0cea-124">If you use the `HubName` attribute in your server-side code, verify that the name used matches the name used to create the hub on the client.</span></span> <span data-ttu-id="a0cea-125">Si vous n’utilisez pas l’attribut, `HubName` vérifiez que le nom du hub d’un client JavaScript est camouflé, comme chatHub au lieu de ChatHub.</span><span class="sxs-lookup"><span data-stu-id="a0cea-125">If you do not use the `HubName` attribute, verify that the name of the hub in a JavaScript client is camel-cased, such as chatHub instead of ChatHub.</span></span>

### <a name="duplicate-method-name-on-client"></a><span data-ttu-id="a0cea-126">Nom de méthode dupliqué sur le client</span><span class="sxs-lookup"><span data-stu-id="a0cea-126">Duplicate method name on client</span></span>

<span data-ttu-id="a0cea-127">Vérifiez que vous n’avez pas de méthode en double sur le client qui ne diffère que par cas.</span><span class="sxs-lookup"><span data-stu-id="a0cea-127">Verify that you do not have a duplicate method on the client that differs only by case.</span></span> <span data-ttu-id="a0cea-128">Si votre application client `sendMessage`a une méthode appelée, vérifiez `SendMessage` qu’il n’y a pas aussi une méthode appelée ainsi.</span><span class="sxs-lookup"><span data-stu-id="a0cea-128">If your client application has a method called `sendMessage`, verify that there isn't also a method called `SendMessage` as well.</span></span>

### <a name="missing-json-parser-on-the-client"></a><span data-ttu-id="a0cea-129">Analyseur JSON manquant sur le client</span><span class="sxs-lookup"><span data-stu-id="a0cea-129">Missing JSON parser on the client</span></span>

<span data-ttu-id="a0cea-130">SignalR exige qu’un analyseur JSON soit présent pour sérialiser les appels entre le serveur et le client.</span><span class="sxs-lookup"><span data-stu-id="a0cea-130">SignalR requires a JSON parser to be present to serialize calls between the server and the client.</span></span> <span data-ttu-id="a0cea-131">Si votre client n’a pas de analyse JSON intégré (comme Internet Explorer 7), vous devrez en inclure un dans votre application.</span><span class="sxs-lookup"><span data-stu-id="a0cea-131">If your client doesn't have a built-in JSON parser (such as Internet Explorer 7), you'll need to include one in your application.</span></span> <span data-ttu-id="a0cea-132">Vous pouvez télécharger le parser JSON [ici](http://nuget.org/packages/json2).</span><span class="sxs-lookup"><span data-stu-id="a0cea-132">You can download the JSON parser [here](http://nuget.org/packages/json2).</span></span>

### <a name="mixing-hub-and-persistentconnection-syntax"></a><span data-ttu-id="a0cea-133">Mixage Hub et Syntaxe PersistentConnection</span><span class="sxs-lookup"><span data-stu-id="a0cea-133">Mixing Hub and PersistentConnection syntax</span></span>

<span data-ttu-id="a0cea-134">SignalR utilise deux modèles de communication : Hubs et PersistentConnections.</span><span class="sxs-lookup"><span data-stu-id="a0cea-134">SignalR uses two communication models: Hubs and PersistentConnections.</span></span> <span data-ttu-id="a0cea-135">La syntaxe pour appeler ces deux modèles de communication est différente dans le code client.</span><span class="sxs-lookup"><span data-stu-id="a0cea-135">The syntax for calling these two communication models is different in the client code.</span></span> <span data-ttu-id="a0cea-136">Si vous avez ajouté un hub dans votre code serveur, vérifiez que tout votre code client utilise la syntaxe appropriée.</span><span class="sxs-lookup"><span data-stu-id="a0cea-136">If you have added a hub in your server code, verify that all of your client code uses the proper hub syntax.</span></span>

<span data-ttu-id="a0cea-137">**Code client JavaScript qui crée une connexion persistante dans un client JavaScript**</span><span class="sxs-lookup"><span data-stu-id="a0cea-137">**JavaScript client code that creates a PersistentConnection in a JavaScript client**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample1.js)]

<span data-ttu-id="a0cea-138">**Code client JavaScript qui crée un proxy Hub dans un client Javascript**</span><span class="sxs-lookup"><span data-stu-id="a0cea-138">**JavaScript client code that creates a Hub Proxy in a Javascript client**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample2.js)]

<span data-ttu-id="a0cea-139">**Code serveur CMD qui cartographie un itinéraire vers une connexion persistante**</span><span class="sxs-lookup"><span data-stu-id="a0cea-139">**C# server code that maps a route to a PersistentConnection**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample3.cs)]

<span data-ttu-id="a0cea-140">**Code serveur CMD qui cartographie un itinéraire vers un Hub, ou vers plusieurs hubs si vous avez plusieurs applications**</span><span class="sxs-lookup"><span data-stu-id="a0cea-140">**C# server code that maps a route to a Hub, or to multiple hubs if you have multiple applications**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample4.cs)]

### <a name="connection-started-before-subscriptions-are-added"></a><span data-ttu-id="a0cea-141">Connexion commencée avant l’ajout d’abonnements</span><span class="sxs-lookup"><span data-stu-id="a0cea-141">Connection started before subscriptions are added</span></span>

<span data-ttu-id="a0cea-142">Si la connexion du Hub est commencée avant que les méthodes qui peuvent être appelées à partir du serveur soient ajoutées au proxy, les messages ne seront pas reçus.</span><span class="sxs-lookup"><span data-stu-id="a0cea-142">If the Hub's connection is started before methods that can be called from the server are added to the proxy, messages will not be received.</span></span> <span data-ttu-id="a0cea-143">Le code JavaScript suivant ne démarre pas correctement le moyeu :</span><span class="sxs-lookup"><span data-stu-id="a0cea-143">The following JavaScript code will not start the hub properly:</span></span>

<span data-ttu-id="a0cea-144">**Code client JavaScript incorrect qui ne permettra pas de recevoir les messages Hubs**</span><span class="sxs-lookup"><span data-stu-id="a0cea-144">**Incorrect JavaScript client code that will not allow Hubs messages to be received**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample5.js)]

<span data-ttu-id="a0cea-145">Au lieu de cela, ajoutez les abonnements de méthode avant d’appeler Démarrer :</span><span class="sxs-lookup"><span data-stu-id="a0cea-145">Instead, add the method subscriptions before calling Start:</span></span>

<span data-ttu-id="a0cea-146">**Code client JavaScript qui ajoute correctement des abonnements à un hub**</span><span class="sxs-lookup"><span data-stu-id="a0cea-146">**JavaScript client code that correctly adds subscriptions to a hub**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample6.js)]

### <a name="missing-method-name-on-the-hub-proxy"></a><span data-ttu-id="a0cea-147">Nom de méthode manquant sur le proxy hub</span><span class="sxs-lookup"><span data-stu-id="a0cea-147">Missing method name on the hub proxy</span></span>

<span data-ttu-id="a0cea-148">Vérifiez que la méthode définie sur le serveur est abonnée sur le client.</span><span class="sxs-lookup"><span data-stu-id="a0cea-148">Verify that the method defined on the server is subscribed to on the client.</span></span> <span data-ttu-id="a0cea-149">Même si le serveur définit la méthode, elle doit tout de même être ajoutée au proxy du client.</span><span class="sxs-lookup"><span data-stu-id="a0cea-149">Even though the server defines the method, it must still be added to the client proxy.</span></span> <span data-ttu-id="a0cea-150">Des méthodes peuvent être ajoutées au proxy du client de `client` la manière suivante (Notez que la méthode est ajoutée au membre du hub, et non directement au moyeu) :</span><span class="sxs-lookup"><span data-stu-id="a0cea-150">Methods can be added to the client proxy in the following ways (Note that the method is added to the `client` member of the hub, not the hub directly):</span></span>

<span data-ttu-id="a0cea-151">**Code client JavaScript qui ajoute des méthodes à un proxy hub**</span><span class="sxs-lookup"><span data-stu-id="a0cea-151">**JavaScript client code that adds methods to a hub proxy**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample7.js)]

### <a name="hub-or-hub-methods-not-declared-as-public"></a><span data-ttu-id="a0cea-152">Méthodes de hub ou de hub non déclarées publiques</span><span class="sxs-lookup"><span data-stu-id="a0cea-152">Hub or hub methods not declared as Public</span></span>

<span data-ttu-id="a0cea-153">Pour être visible sur le client, la mise `public`en œuvre et les méthodes du hub doivent être déclarées comme .</span><span class="sxs-lookup"><span data-stu-id="a0cea-153">To be visible on the client, the hub implementation and methods must be declared as `public`.</span></span>

### <a name="accessing-hub-from-a-different-application"></a><span data-ttu-id="a0cea-154">Accès au hub à partir d’une application différente</span><span class="sxs-lookup"><span data-stu-id="a0cea-154">Accessing hub from a different application</span></span>

<span data-ttu-id="a0cea-155">Les hubs SignalR ne peuvent être consultés que par le biais d’applications qui implémentent les clients SignalR.</span><span class="sxs-lookup"><span data-stu-id="a0cea-155">SignalR Hubs can only be accessed through applications that implement SignalR clients.</span></span> <span data-ttu-id="a0cea-156">SignalR ne peut pas interopérer avec d’autres bibliothèques de communication (comme les services Web SOAP ou WCF).) S’il n’y a pas de client SignalR disponible pour votre plate-forme cible, vous ne pouvez pas accéder directement au point de terminaison du serveur.</span><span class="sxs-lookup"><span data-stu-id="a0cea-156">SignalR cannot interoperate with other communication libraries (like SOAP or WCF web services.) If there is no SignalR client available for your target platform, you cannot access the server's endpoint directly.</span></span>

### <a name="manually-serializing-data"></a><span data-ttu-id="a0cea-157">Données sérialisant manuellement</span><span class="sxs-lookup"><span data-stu-id="a0cea-157">Manually serializing data</span></span>

<span data-ttu-id="a0cea-158">SignalR utilisera automatiquement JSON pour sérialiser vos paramètres de méthode, il n’est pas nécessaire de le faire vous-même.</span><span class="sxs-lookup"><span data-stu-id="a0cea-158">SignalR will automatically use JSON to serialize your method parameters- there's no need to do it yourself.</span></span>

### <a name="remote-hub-method-not-executed-on-client-in-ondisconnected-function"></a><span data-ttu-id="a0cea-159">Méthode Remote Hub non exécutée sur le client dans la fonction OnDisconnected</span><span class="sxs-lookup"><span data-stu-id="a0cea-159">Remote Hub method not executed on client in OnDisconnected function</span></span>

<span data-ttu-id="a0cea-160">Ce comportement est normal.</span><span class="sxs-lookup"><span data-stu-id="a0cea-160">This behavior is by design.</span></span> <span data-ttu-id="a0cea-161">Lorsqu’il `OnDisconnected` est appelé, le `Disconnected` hub est déjà entré dans l’état, ce qui ne permet pas d’appeler d’autres méthodes de moyeu.</span><span class="sxs-lookup"><span data-stu-id="a0cea-161">When `OnDisconnected` is called, the hub has already entered the `Disconnected` state, which does not allow further hub methods to be called.</span></span>

<span data-ttu-id="a0cea-162">**Code serveur CMD qui exécute correctement le code dans l’événement OnDisconnected**</span><span class="sxs-lookup"><span data-stu-id="a0cea-162">**C# server code that correctly executes code in the OnDisconnected event**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample8.cs)]

### <a name="connection-limit-reached"></a><span data-ttu-id="a0cea-163">Limite de connexion atteinte</span><span class="sxs-lookup"><span data-stu-id="a0cea-163">Connection limit reached</span></span>

<span data-ttu-id="a0cea-164">Lors de l’utilisation de la version complète de l’IIS sur un système d’exploitation client comme Windows 7, une limite de 10 connexions est imposée.</span><span class="sxs-lookup"><span data-stu-id="a0cea-164">When using the full version of IIS on a client operating system like Windows 7, a 10-connection limit is imposed.</span></span> <span data-ttu-id="a0cea-165">Lorsque vous utilisez un système d’exploitation client, utilisez IIS Express plutôt pour éviter cette limite.</span><span class="sxs-lookup"><span data-stu-id="a0cea-165">When using a client OS, use IIS Express instead to avoid this limit.</span></span>

### <a name="cross-domain-connection-not-set-up-properly"></a><span data-ttu-id="a0cea-166">Connexion cross-domain non configuré correctement</span><span class="sxs-lookup"><span data-stu-id="a0cea-166">Cross-domain connection not set up properly</span></span>

<span data-ttu-id="a0cea-167">Si une connexion cross-domain (une connexion pour laquelle l’URL SignalR n’est pas dans le même domaine que la page d’hébergement) n’est pas configurée correctement, la connexion peut échouer sans un message d’erreur.</span><span class="sxs-lookup"><span data-stu-id="a0cea-167">If a cross-domain connection (a connection for which the SignalR URL is not in the same domain as the hosting page) is not set up correctly, the connection may fail without an error message.</span></span> <span data-ttu-id="a0cea-168">Pour plus d’informations sur la façon d’activer la communication inter-domaines, voir [Comment établir une connexion inter-domaines](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span><span class="sxs-lookup"><span data-stu-id="a0cea-168">For information on how to enable cross-domain communication, see [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span></span>

### <a name="connection-using-ntlm-active-directory-not-working-in-net-client"></a><span data-ttu-id="a0cea-169">Connexion à l’aide de NTLM (Active Directory) ne fonctionne pas dans le client .NET</span><span class="sxs-lookup"><span data-stu-id="a0cea-169">Connection using NTLM (Active Directory) not working in .NET client</span></span>

<span data-ttu-id="a0cea-170">Une connexion dans une application client .NET qui utilise la sécurité de Domaine peut échouer si la connexion n’est pas configurée correctement.</span><span class="sxs-lookup"><span data-stu-id="a0cea-170">A connection in a .NET client application that uses Domain security may fail if the connection is not configured properly.</span></span> <span data-ttu-id="a0cea-171">Pour utiliser SignalR dans un environnement de domaine, définissez la propriété de connexion requise comme suit :</span><span class="sxs-lookup"><span data-stu-id="a0cea-171">To use SignalR in a domain environment, set the requisite connection property as follows:</span></span>

<span data-ttu-id="a0cea-172">**Code client CMD qui implémente les informations d’identification de connexion**</span><span class="sxs-lookup"><span data-stu-id="a0cea-172">**C# client code that implements connection credentials**</span></span>

[!code-csharp[Main](troubleshooting/samples/sample9.cs)]

<a id="other"></a>

## <a name="other-connection-issues"></a><span data-ttu-id="a0cea-173">Autres problèmes de connexion</span><span class="sxs-lookup"><span data-stu-id="a0cea-173">Other connection issues</span></span>

<span data-ttu-id="a0cea-174">Cette section décrit les causes et les solutions pour des symptômes ou des messages d’erreur spécifiques qui se produisent lors d’une connexion.</span><span class="sxs-lookup"><span data-stu-id="a0cea-174">This section describes the causes and solutions for specific symptoms or error messages that occur during a connection.</span></span>

### <a name="start-must-be-called-before-data-can-be-sent-error"></a><span data-ttu-id="a0cea-175">Erreur de « Démarrer avant que les données puissent être envoyées »</span><span class="sxs-lookup"><span data-stu-id="a0cea-175">"Start must be called before data can be sent" error</span></span>

<span data-ttu-id="a0cea-176">Cette erreur est généralement visible si le code fait référence aux objets SignalR avant le début de la connexion.</span><span class="sxs-lookup"><span data-stu-id="a0cea-176">This error is commonly seen if code references SignalR objects before the connection is started.</span></span> <span data-ttu-id="a0cea-177">L’écoute électronique pour les gestionnaires et autres que les méthodes d’appel définies sur le serveur doivent être ajoutées après la fin de la connexion.</span><span class="sxs-lookup"><span data-stu-id="a0cea-177">The wireup for handlers and the like that will call methods defined on the server must be added after the connection completes.</span></span> <span data-ttu-id="a0cea-178">Notez que `Start` l’appel à est asynchrone, de sorte que le code après l’appel peut être exécuté avant qu’il ne se termine.</span><span class="sxs-lookup"><span data-stu-id="a0cea-178">Note that the call to `Start` is asynchronous, so code after the call may be executed before it completes.</span></span> <span data-ttu-id="a0cea-179">La meilleure façon d’ajouter des gestionnaires après une connexion commence complètement est de les mettre dans une fonction de rappel qui est passé comme un paramètre à la méthode de départ:</span><span class="sxs-lookup"><span data-stu-id="a0cea-179">The best way to add handlers after a connection starts completely is to put them into a callback function that is passed as a parameter to the start method:</span></span>

<span data-ttu-id="a0cea-180">**Code client JavaScript qui ajoute correctement les gestionnaires d’événements qui font référence aux objets SignalR**</span><span class="sxs-lookup"><span data-stu-id="a0cea-180">**JavaScript client code that correctly adds event handlers that reference SignalR objects**</span></span>

[!code-javascript[Main](troubleshooting/samples/sample10.js?highlight=1)]

<span data-ttu-id="a0cea-181">Cette erreur sera également visible si une connexion s’arrête pendant que les objets SignalR sont toujours référencés.</span><span class="sxs-lookup"><span data-stu-id="a0cea-181">This error will also be seen if a connection stops while SignalR objects are still being referenced.</span></span>

### <a name="301-moved-permanently-or-302-moved-temporarily-error"></a><span data-ttu-id="a0cea-182">Erreur "301 déplacée de façon permanente" ou "302 déplacé temporairement"</span><span class="sxs-lookup"><span data-stu-id="a0cea-182">"301 Moved Permanently" or "302 Moved Temporarily" error</span></span>

<span data-ttu-id="a0cea-183">Cette erreur peut être vue si le projet contient un dossier appelé SignalR, ce qui interfère avec le proxy créé automatiquement.</span><span class="sxs-lookup"><span data-stu-id="a0cea-183">This error may be seen if the project contains a folder called SignalR, which will interfere with the automatically-created proxy.</span></span> <span data-ttu-id="a0cea-184">Pour éviter cette erreur, n’utilisez `SignalR` pas de dossier appelé dans votre application, ou éteignez la génération de proxy automatique.</span><span class="sxs-lookup"><span data-stu-id="a0cea-184">To avoid this error, do not use a folder called `SignalR` in your application, or turn automatic proxy generation off.</span></span> <span data-ttu-id="a0cea-185">Voir [The Generated Proxy et ce qu’il fait pour vous](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy) pour plus de détails.</span><span class="sxs-lookup"><span data-stu-id="a0cea-185">See [The Generated Proxy and what it does for you](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy) for more details.</span></span>

### <a name="403-forbidden-error-in-net-or-silverlight-client"></a><span data-ttu-id="a0cea-186">Erreur "403 Interdit" dans .NET ou Silverlight client</span><span class="sxs-lookup"><span data-stu-id="a0cea-186">"403 Forbidden" error in .NET or Silverlight client</span></span>

<span data-ttu-id="a0cea-187">Cette erreur peut se produire dans des environnements trans-domaines où la communication inter-domaines n’est pas correctement activée.</span><span class="sxs-lookup"><span data-stu-id="a0cea-187">This error may occur in cross-domain environments where cross-domain communication is not properly enabled.</span></span> <span data-ttu-id="a0cea-188">Pour plus d’informations sur la façon d’activer la communication inter-domaines, voir [Comment établir une connexion inter-domaines](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span><span class="sxs-lookup"><span data-stu-id="a0cea-188">For information on how to enable cross-domain communication, see [How to establish a cross-domain connection](../guide-to-the-api/hubs-api-guide-javascript-client.md#crossdomain).</span></span> <span data-ttu-id="a0cea-189">Pour établir une connexion cross-domain dans un client Silverlight, voir [les connexions Cross-domain des clients Silverlight](../guide-to-the-api/hubs-api-guide-net-client.md#slcrossdomain).</span><span class="sxs-lookup"><span data-stu-id="a0cea-189">To establish a cross-domain connection in a Silverlight client, see [Cross-domain connections from Silverlight clients](../guide-to-the-api/hubs-api-guide-net-client.md#slcrossdomain).</span></span>

### <a name="404-not-found-error"></a><span data-ttu-id="a0cea-190">Erreur "404 Non trouvée"</span><span class="sxs-lookup"><span data-stu-id="a0cea-190">"404 Not Found" error</span></span>

<span data-ttu-id="a0cea-191">Il y a plusieurs causes à ce problème.</span><span class="sxs-lookup"><span data-stu-id="a0cea-191">There are several causes for this issue.</span></span> <span data-ttu-id="a0cea-192">Vérifier tous les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="a0cea-192">Verify all of the following:</span></span>

- <span data-ttu-id="a0cea-193">**Référence d’adresse de proxy Hub non formatée correctement :** Cette erreur est généralement visible si la référence à l’adresse proxy hub générée n’est pas formatée correctement.</span><span class="sxs-lookup"><span data-stu-id="a0cea-193">**Hub proxy address reference not formatted correctly:** This error is commonly seen if the reference to the generated hub proxy address is not formatted correctly.</span></span> <span data-ttu-id="a0cea-194">Vérifiez que la référence à l’adresse du hub est faite correctement.</span><span class="sxs-lookup"><span data-stu-id="a0cea-194">Verify that the reference to the hub address is made properly.</span></span> <span data-ttu-id="a0cea-195">Voir [comment faire référence au proxy généré dynamiquement](../guide-to-the-api/hubs-api-guide-javascript-client.md#dynamicproxy) pour plus de détails.</span><span class="sxs-lookup"><span data-stu-id="a0cea-195">See [How to reference the dynamically generated proxy](../guide-to-the-api/hubs-api-guide-javascript-client.md#dynamicproxy) for details.</span></span>
- <span data-ttu-id="a0cea-196">**Ajout d’itinéraires à l’application avant d’ajouter l’itinéraire hub :** Si votre application utilise d’autres itinéraires, vérifiez `MapHubs`que le premier itinéraire ajouté est l’appel à .</span><span class="sxs-lookup"><span data-stu-id="a0cea-196">**Adding routes to application before adding the hub route:** If your application uses other routes, verify that the first route added is the call to `MapHubs`.</span></span>

### <a name="500-internal-server-error"></a><span data-ttu-id="a0cea-197">"500 Erreur de serveur interne"</span><span class="sxs-lookup"><span data-stu-id="a0cea-197">"500 Internal Server Error"</span></span>

<span data-ttu-id="a0cea-198">Il s’agit d’une erreur très générique qui pourrait avoir une grande variété de causes.</span><span class="sxs-lookup"><span data-stu-id="a0cea-198">This is a very generic error that could have a wide variety of causes.</span></span> <span data-ttu-id="a0cea-199">Les détails de l’erreur doivent apparaître dans le journal de l’événement du serveur, ou peuvent être trouvés en débogage du serveur.</span><span class="sxs-lookup"><span data-stu-id="a0cea-199">The details of the error should appear in the server's event log, or can be found through debugging the server.</span></span> <span data-ttu-id="a0cea-200">Des informations d’erreur plus détaillées peuvent être obtenues en allumant des erreurs détaillées sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="a0cea-200">More detailed error information may be obtained by turning on detailed errors on the server.</span></span> <span data-ttu-id="a0cea-201">Pour plus d’informations, voir [Comment gérer les erreurs dans la classe Hub](../guide-to-the-api/hubs-api-guide-server.md#handleErrors).</span><span class="sxs-lookup"><span data-stu-id="a0cea-201">For more information, see [How to handle errors in the Hub class](../guide-to-the-api/hubs-api-guide-server.md#handleErrors).</span></span>

### <a name="typeerror-lthubtypegt-is-undefined-error"></a><span data-ttu-id="a0cea-202">Erreur "TypeError: &lt;&gt; hubType n’est pas définie"</span><span class="sxs-lookup"><span data-stu-id="a0cea-202">"TypeError: &lt;hubType&gt; is undefined" error</span></span>

<span data-ttu-id="a0cea-203">Cette erreur se traduira `MapHubs` si l’appel à n’est pas fait correctement.</span><span class="sxs-lookup"><span data-stu-id="a0cea-203">This error will result if the call to `MapHubs` is not made properly.</span></span> <span data-ttu-id="a0cea-204">Découvrez [comment enregistrer l’itinéraire SignalR et configurer les options SignalR](../guide-to-the-api/hubs-api-guide-server.md#route) pour plus d’informations.</span><span class="sxs-lookup"><span data-stu-id="a0cea-204">See [How to register the SignalR route and configure SignalR options](../guide-to-the-api/hubs-api-guide-server.md#route) for more information.</span></span>

### <a name="jsonserializationexception-was-unhandled-by-user-code"></a><span data-ttu-id="a0cea-205">JsonSerializationException n’a pas été géré par le code utilisateur</span><span class="sxs-lookup"><span data-stu-id="a0cea-205">JsonSerializationException was unhandled by user code</span></span>

<span data-ttu-id="a0cea-206">Vérifiez que les paramètres que vous envoyez à vos méthodes n’incluent pas les types non sérialisables (comme les poignées de fichiers ou les connexions de base de données).</span><span class="sxs-lookup"><span data-stu-id="a0cea-206">Verify that the parameters you send to your methods do not include non-serializable types (like file handles or database connections).</span></span> <span data-ttu-id="a0cea-207">Si vous devez utiliser des membres sur un objet côté serveur que vous ne souhaitez pas être envoyé au `JSONIgnore` client (pour la sécurité ou pour des raisons de sérialisation), utilisez l’attribut.</span><span class="sxs-lookup"><span data-stu-id="a0cea-207">If you need to use members on a server-side object that you don't want to be sent to the client (either for security or for reasons of serialization), use the `JSONIgnore` attribute.</span></span>

### <a name="protocol-error-unknown-transport-error"></a><span data-ttu-id="a0cea-208">Erreur de protocole : erreur de transport inconnu</span><span class="sxs-lookup"><span data-stu-id="a0cea-208">"Protocol error: Unknown transport" error</span></span>

<span data-ttu-id="a0cea-209">Cette erreur peut se produire si le client ne prend pas en charge les transports que SignalR utilise.</span><span class="sxs-lookup"><span data-stu-id="a0cea-209">This error may occur if the client does not support the transports that SignalR uses.</span></span> <span data-ttu-id="a0cea-210">Consultez [Transports et Fallbacks](../getting-started/introduction-to-signalr.md#transports) pour plus d’informations sur les navigateurs qui peuvent être utilisés avec SignalR.</span><span class="sxs-lookup"><span data-stu-id="a0cea-210">See [Transports and Fallbacks](../getting-started/introduction-to-signalr.md#transports) for information on which browsers can be used with SignalR.</span></span>

### <a name="javascript-hub-proxy-generation-has-been-disabled"></a><span data-ttu-id="a0cea-211">"JavaScript Hub proxy generation has been disabled."</span><span class="sxs-lookup"><span data-stu-id="a0cea-211">"JavaScript Hub proxy generation has been disabled."</span></span>

<span data-ttu-id="a0cea-212">Cette erreur se `DisableJavaScriptProxies` produira si elle est définie tout en `signalr/hubs`incluant une référence au proxy généré dynamiquement à .</span><span class="sxs-lookup"><span data-stu-id="a0cea-212">This error will occur if `DisableJavaScriptProxies` is set while also including a reference to the dynamically generated proxy at `signalr/hubs`.</span></span> <span data-ttu-id="a0cea-213">Pour plus d’informations sur la création de la procuration manuellement, voir [Le proxy généré et ce qu’il fait pour vous](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span><span class="sxs-lookup"><span data-stu-id="a0cea-213">For more information on creating the proxy manually, see [The generated proxy and what it does for you](../guide-to-the-api/hubs-api-guide-javascript-client.md#genproxy).</span></span>

### <a name="the-connection-id-is-in-the-incorrect-format-or-the-user-identity-cannot-change-during-an-active-signalr-connection-error"></a><span data-ttu-id="a0cea-214">"L’ID de connexion est dans le format incorrect" ou "L’identité de l’utilisateur ne peut pas changer lors d’une connexion SignalR active" erreur</span><span class="sxs-lookup"><span data-stu-id="a0cea-214">"The connection ID is in the incorrect format" or "The user identity cannot change during an active SignalR connection" error</span></span>

<span data-ttu-id="a0cea-215">Cette erreur peut être vue si l’authentification est utilisée, et le client est déconnecté avant que la connexion soit arrêtée.</span><span class="sxs-lookup"><span data-stu-id="a0cea-215">This error may be seen if authentication is being used, and the client is logged out before the connection is stopped.</span></span> <span data-ttu-id="a0cea-216">La solution est d’arrêter la connexion SignalR avant d’enregistrer le client.</span><span class="sxs-lookup"><span data-stu-id="a0cea-216">The solution is to stop the SignalR connection before logging the client out.</span></span>

### <a name="uncaught-error-signalr-jquery-not-found-please-ensure-jquery-is-referenced-before-the-signalrjs-file-error"></a><span data-ttu-id="a0cea-217">"Erreur non-tendue: SignalR: jQuery pas trouvé.</span><span class="sxs-lookup"><span data-stu-id="a0cea-217">"Uncaught Error: SignalR: jQuery not found.</span></span> <span data-ttu-id="a0cea-218">S’il vous plaît assurez-vous jQuery est référencé avant le fichier SignalR.js" erreur</span><span class="sxs-lookup"><span data-stu-id="a0cea-218">Please ensure jQuery is referenced before the SignalR.js file" error</span></span>

<span data-ttu-id="a0cea-219">Le client SignalR JavaScript nécessite jQuery pour fonctionner.</span><span class="sxs-lookup"><span data-stu-id="a0cea-219">The SignalR JavaScript client requires jQuery to run.</span></span> <span data-ttu-id="a0cea-220">Vérifiez que votre référence à jQuery est correcte, que le chemin utilisé est valide, et que la référence à jQuery est avant la référence à SignalR.</span><span class="sxs-lookup"><span data-stu-id="a0cea-220">Verify that your reference to jQuery is correct, that the path used is valid, and that the reference to jQuery is before the reference to SignalR.</span></span>

### <a name="uncaught-typeerror-cannot-read-property-ltpropertygt-of-undefined-error"></a><span data-ttu-id="a0cea-221">"Uncaught TypeError: Impossible&lt;de&gt;lire la propriété ' propriété ' de non défini" erreur</span><span class="sxs-lookup"><span data-stu-id="a0cea-221">"Uncaught TypeError: Cannot read property '&lt;property&gt;' of undefined" error</span></span>

<span data-ttu-id="a0cea-222">Cette erreur résulte de ne pas avoir jQuery ou les hubs proxy référencé correctement.</span><span class="sxs-lookup"><span data-stu-id="a0cea-222">This error results from not having jQuery or the hubs proxy referenced properly.</span></span> <span data-ttu-id="a0cea-223">Vérifiez que votre référence à jQuery et le proxy des hubs est correcte, que le chemin utilisé est valide, et que la référence à jQuery est avant la référence au proxy des hubs.</span><span class="sxs-lookup"><span data-stu-id="a0cea-223">Verify that your reference to jQuery and the hubs proxy is correct, that the path used is valid, and that the reference to jQuery is before the reference to the hubs proxy.</span></span> <span data-ttu-id="a0cea-224">La référence par défaut au proxy des hubs doit ressembler à ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="a0cea-224">The default reference to the hubs proxy should look like the following:</span></span>

<span data-ttu-id="a0cea-225">**Code HTML côté client qui fait correctement référence au proxy Hubs**</span><span class="sxs-lookup"><span data-stu-id="a0cea-225">**HTML client-side code that correctly references the Hubs proxy**</span></span>

[!code-html[Main](troubleshooting/samples/sample11.html)]

### <a name="runtimebinderexception-was-unhandled-by-user-code-error"></a><span data-ttu-id="a0cea-226">"RuntimeBinderException n’a pas été géré par le code utilisateur" erreur</span><span class="sxs-lookup"><span data-stu-id="a0cea-226">"RuntimeBinderException was unhandled by user code" error</span></span>

<span data-ttu-id="a0cea-227">Cette erreur peut se produire `Hub.On` lorsque la surcharge incorrecte est utilisée.</span><span class="sxs-lookup"><span data-stu-id="a0cea-227">This error may occur when the incorrect overload of `Hub.On` is used.</span></span> <span data-ttu-id="a0cea-228">Si la méthode a une valeur de retour, le type de rendement doit être spécifié comme paramètre de type générique :</span><span class="sxs-lookup"><span data-stu-id="a0cea-228">If the method has a return value, the return type must be specified as a generic type parameter:</span></span>

<span data-ttu-id="a0cea-229">**Méthode définie sur le client (sans proxy généré)**</span><span class="sxs-lookup"><span data-stu-id="a0cea-229">**Method defined on the client (without generated proxy)**</span></span>

[!code-html[Main](troubleshooting/samples/sample12.html?highlight=1)]

### <a name="connection-id-is-inconsistent-or-connection-breaks-between-page-loads"></a><span data-ttu-id="a0cea-230">L’id de connexion est incohérent ou les ruptures de connexion entre les charges de page</span><span class="sxs-lookup"><span data-stu-id="a0cea-230">Connection ID is inconsistent or connection breaks between page loads</span></span>

<span data-ttu-id="a0cea-231">Ce comportement est normal.</span><span class="sxs-lookup"><span data-stu-id="a0cea-231">This behavior is by design.</span></span> <span data-ttu-id="a0cea-232">Étant donné que l’objet hub est hébergé dans l’objet de la page, le moyeu est détruit lorsque la page se actualise.</span><span class="sxs-lookup"><span data-stu-id="a0cea-232">Since the hub object is hosted in the page object, the hub is destroyed when the page refreshes.</span></span> <span data-ttu-id="a0cea-233">Une application de plusieurs pages doit maintenir l’association entre les utilisateurs et les ID de connexion afin qu’ils soient cohérents entre les charges de page.</span><span class="sxs-lookup"><span data-stu-id="a0cea-233">A multi-page application needs to maintain the association between users and connection IDs so that they will be consistent between page loads.</span></span> <span data-ttu-id="a0cea-234">Les pièces d’ID de connexion peuvent `ConcurrentDictionary` être stockées sur le serveur dans un objet ou une base de données.</span><span class="sxs-lookup"><span data-stu-id="a0cea-234">The connection IDs can be stored on the server in either a `ConcurrentDictionary` object or a database.</span></span>

### <a name="value-cannot-be-null-error"></a><span data-ttu-id="a0cea-235">Erreur de « valeur ne peut pas être nulle »</span><span class="sxs-lookup"><span data-stu-id="a0cea-235">"Value cannot be null" error</span></span>

<span data-ttu-id="a0cea-236">Les méthodes côté serveur avec paramètres facultatifs ne sont pas actuellement prises en charge; si le paramètre facultatif est omis, la méthode échouera.</span><span class="sxs-lookup"><span data-stu-id="a0cea-236">Server-side methods with optional parameters are not currently supported; if the optional parameter is omitted, the method will fail.</span></span> <span data-ttu-id="a0cea-237">Pour plus d’informations, voir [Paramètres facultatifs](https://github.com/SignalR/SignalR/issues/324).</span><span class="sxs-lookup"><span data-stu-id="a0cea-237">For more information, see [Optional Parameters](https://github.com/SignalR/SignalR/issues/324).</span></span>

### <a name="firefox-cant-establish-a-connection-to-the-server-at-ltaddressgt-error-in-firebug"></a><span data-ttu-id="a0cea-238">"Firefox ne peut pas établir une &lt;&gt;connexion au serveur à l’adresse" erreur dans Firebug</span><span class="sxs-lookup"><span data-stu-id="a0cea-238">"Firefox can't establish a connection to the server at &lt;address&gt;" error in Firebug</span></span>

<span data-ttu-id="a0cea-239">Ce message d’erreur peut être vu dans Firebug si la négociation du transport WebSocket échoue et un autre transport est utilisé à la place.</span><span class="sxs-lookup"><span data-stu-id="a0cea-239">This error message can be seen in Firebug if negotiation of the WebSocket transport fails and another transport is used instead.</span></span> <span data-ttu-id="a0cea-240">Ce comportement est normal.</span><span class="sxs-lookup"><span data-stu-id="a0cea-240">This behavior is by design.</span></span>

### <a name="the-remote-certificate-is-invalid-according-to-the-validation-procedure-error-in-net-client-application"></a><span data-ttu-id="a0cea-241">"Le certificat à distance est invalide selon la procédure de validation" erreur dans l’application client .NET</span><span class="sxs-lookup"><span data-stu-id="a0cea-241">"The remote certificate is invalid according to the validation procedure" error in .NET client application</span></span>

<span data-ttu-id="a0cea-242">Si votre serveur nécessite des certificats clients personnalisés, vous pouvez ajouter un x509certificate à la connexion avant que la demande ne soit faite.</span><span class="sxs-lookup"><span data-stu-id="a0cea-242">If your server requires custom client certificates, then you can add an x509certificate to the connection before the request is made.</span></span> <span data-ttu-id="a0cea-243">Ajouter le certificat à `Connection.AddClientCertificate`la connexion à l’aide de .</span><span class="sxs-lookup"><span data-stu-id="a0cea-243">Add the certificate to the connection using `Connection.AddClientCertificate`.</span></span>

### <a name="connection-drops-after-authentication-times-out"></a><span data-ttu-id="a0cea-244">La connexion diminue après les temps d’authentification</span><span class="sxs-lookup"><span data-stu-id="a0cea-244">Connection drops after authentication times out</span></span>

<span data-ttu-id="a0cea-245">Ce comportement est normal.</span><span class="sxs-lookup"><span data-stu-id="a0cea-245">This behavior is by design.</span></span> <span data-ttu-id="a0cea-246">Les informations d’authentification ne peuvent pas être modifiées pendant qu’une connexion est active; pour actualiser les informations d’identification, la connexion doit être arrêtée et redémarrée.</span><span class="sxs-lookup"><span data-stu-id="a0cea-246">Authentication credentials cannot be modified while a connection is active; to refresh credentials, the connection must be stopped and restarted.</span></span>

### <a name="onconnected-gets-called-twice-when-using-jquery-mobile"></a><span data-ttu-id="a0cea-247">OnConnected est appelé deux fois lors de l’utilisation de jQuery Mobile</span><span class="sxs-lookup"><span data-stu-id="a0cea-247">OnConnected gets called twice when using jQuery Mobile</span></span>

<span data-ttu-id="a0cea-248">La fonction de `initializePage` jQuery Mobile force les scripts de chaque page à être réécrits, créant ainsi une deuxième connexion.</span><span class="sxs-lookup"><span data-stu-id="a0cea-248">jQuery Mobile's `initializePage` function forces the scripts in each page to be re-executed, thus creating a second connection.</span></span> <span data-ttu-id="a0cea-249">Les solutions à ce problème comprennent :</span><span class="sxs-lookup"><span data-stu-id="a0cea-249">Solutions for this issue include:</span></span>

- <span data-ttu-id="a0cea-250">Inclure la référence à jQuery Mobile avant votre fichier JavaScript.</span><span class="sxs-lookup"><span data-stu-id="a0cea-250">Include the reference to jQuery Mobile before your JavaScript file.</span></span>
- <span data-ttu-id="a0cea-251">Désactiver la `initializePage` fonction `$.mobile.autoInitializePage = false`en définissant .</span><span class="sxs-lookup"><span data-stu-id="a0cea-251">Disable the `initializePage` function by setting `$.mobile.autoInitializePage = false`.</span></span>
- <span data-ttu-id="a0cea-252">Attendez que la page termine l’initialisation avant de commencer la connexion.</span><span class="sxs-lookup"><span data-stu-id="a0cea-252">Wait for the page to finish initializing before starting the connection.</span></span>

### <a name="messages-are-delayed-in-silverlight-applications-using-server-sent-events"></a><span data-ttu-id="a0cea-253">Les messages sont retardés dans les applications Silverlight à l’aide d’événements Envoyés par serveur</span><span class="sxs-lookup"><span data-stu-id="a0cea-253">Messages are delayed in Silverlight applications using Server Sent Events</span></span>

<span data-ttu-id="a0cea-254">Les messages sont retardés lors de l’utilisation du serveur envoyé des événements sur Silverlight.</span><span class="sxs-lookup"><span data-stu-id="a0cea-254">Messages are delayed when using server sent events on Silverlight.</span></span> <span data-ttu-id="a0cea-255">Pour forcer l’utilisation de longs sondages, utilisez ce qui suit au démarrage de la connexion :</span><span class="sxs-lookup"><span data-stu-id="a0cea-255">To force long polling to be used instead, use the following when starting the connection:</span></span>

[!code-css[Main](troubleshooting/samples/sample13.css)]

### <a name="permission-denied-using-forever-frame-protocol"></a><span data-ttu-id="a0cea-256">"Permission refusée" en utilisant le protocole Forever Frame</span><span class="sxs-lookup"><span data-stu-id="a0cea-256">"Permission Denied" using Forever Frame protocol</span></span>

<span data-ttu-id="a0cea-257">Il s’agit d’un problème connu, décrit [ici](https://github.com/SignalR/SignalR/issues/1963).</span><span class="sxs-lookup"><span data-stu-id="a0cea-257">This is a known issue, described [here](https://github.com/SignalR/SignalR/issues/1963).</span></span> <span data-ttu-id="a0cea-258">Ce symptôme peut être vu à l’aide de la dernière bibliothèque JQuery; la solution de contournement est de rétrograder votre demande à JQuery 1.8.2.</span><span class="sxs-lookup"><span data-stu-id="a0cea-258">This symptom may be seen using the latest JQuery library; the workaround is to downgrade your application to JQuery 1.8.2.</span></span>

<a id="server"></a>

## <a name="compilation-and-server-side-errors"></a><span data-ttu-id="a0cea-259">Compilation et erreurs côté serveur</span><span class="sxs-lookup"><span data-stu-id="a0cea-259">Compilation and server-side errors</span></span>

 <span data-ttu-id="a0cea-260">La section suivante contient des solutions possibles pour compiler et côté serveur des erreurs d’exécution.</span><span class="sxs-lookup"><span data-stu-id="a0cea-260">The following section contains possible solutions to compiler and server-side runtime errors.</span></span> 

### <a name="reference-to-hub-instance-is-null"></a><span data-ttu-id="a0cea-261">La référence à l’instance Hub est nulle</span><span class="sxs-lookup"><span data-stu-id="a0cea-261">Reference to Hub instance is null</span></span>

<span data-ttu-id="a0cea-262">Étant donné qu’une instance de hub est créée pour chaque connexion, vous ne pouvez pas créer une instance d’un hub dans votre code vous-même.</span><span class="sxs-lookup"><span data-stu-id="a0cea-262">Since a hub instance is created for each connection, you can't create an instance of a hub in your code yourself.</span></span> <span data-ttu-id="a0cea-263">Pour appeler les méthodes sur un hub de l’extérieur du hub lui-même, voir [Comment appeler les méthodes des clients et gérer les groupes de l’extérieur de la classe Hub](../guide-to-the-api/hubs-api-guide-server.md#callfromoutsidehub) pour obtenir une référence au contexte du hub.</span><span class="sxs-lookup"><span data-stu-id="a0cea-263">To call methods on a hub from outside the hub itself, see [How to call client methods and manage groups from outside the Hub class](../guide-to-the-api/hubs-api-guide-server.md#callfromoutsidehub) for how to obtain a reference to the hub context.</span></span>

### <a name="httpcontextcurrentsession-is-null"></a><span data-ttu-id="a0cea-264">HTTPContext.Current.Session est nul</span><span class="sxs-lookup"><span data-stu-id="a0cea-264">HTTPContext.Current.Session is null</span></span>

<span data-ttu-id="a0cea-265">Ce comportement est normal.</span><span class="sxs-lookup"><span data-stu-id="a0cea-265">This behavior is by design.</span></span> <span data-ttu-id="a0cea-266">SignalR ne prend pas en charge l’état de session ASP.NET, car permettre à l’état de session briserait la messagerie duplex.</span><span class="sxs-lookup"><span data-stu-id="a0cea-266">SignalR does not support the ASP.NET session state, since enabling the session state would break duplex messaging.</span></span>

### <a name="no-suitable-method-to-override"></a><span data-ttu-id="a0cea-267">Aucune méthode appropriée pour remplacer</span><span class="sxs-lookup"><span data-stu-id="a0cea-267">No suitable method to override</span></span>

<span data-ttu-id="a0cea-268">Vous pouvez voir cette erreur si vous utilisez le code de la documentation plus ancienne ou des blogs.</span><span class="sxs-lookup"><span data-stu-id="a0cea-268">You may see this error if you are using code from older documentation or blogs.</span></span> <span data-ttu-id="a0cea-269">Vérifiez que vous ne faites pas référence aux noms des méthodes `OnConnectedAsync`qui ont été modifiées ou dépréciées (comme).</span><span class="sxs-lookup"><span data-stu-id="a0cea-269">Verify that you are not referencing names of methods that have been changed or deprecated (like `OnConnectedAsync`).</span></span>

### <a name="hostcontextextensionswebsocketserverurl-is-null"></a><span data-ttu-id="a0cea-270">HostContextExtensions.WebSocketServerUrl est nul</span><span class="sxs-lookup"><span data-stu-id="a0cea-270">HostContextExtensions.WebSocketServerUrl is null</span></span>

<span data-ttu-id="a0cea-271">Ce comportement est normal.</span><span class="sxs-lookup"><span data-stu-id="a0cea-271">This behavior is by design.</span></span> <span data-ttu-id="a0cea-272">Ce membre est déprécié et ne doit pas être utilisé.</span><span class="sxs-lookup"><span data-stu-id="a0cea-272">This member is deprecated and should not be used.</span></span>

### <a name="a-route-named-signalrhubs-is-already-in-the-route-collection-error"></a><span data-ttu-id="a0cea-273">"Un itinéraire nommé 'signalr.hubs' est déjà dans la collection d’itinéraires" erreur</span><span class="sxs-lookup"><span data-stu-id="a0cea-273">"A route named 'signalr.hubs' is already in the route collection" error</span></span>

<span data-ttu-id="a0cea-274">Cette erreur sera `MapHubs` vue si elle est appelée deux fois par votre application.</span><span class="sxs-lookup"><span data-stu-id="a0cea-274">This error will be seen if `MapHubs` is called twice by your application.</span></span> <span data-ttu-id="a0cea-275">Certains exemples `MapHubs` d’applications sont directement dans le fichier de demande global; d’autres font l’appel dans une classe d’emballage.</span><span class="sxs-lookup"><span data-stu-id="a0cea-275">Some example applications call `MapHubs` directly in the global application file; others make the call in a wrapper class.</span></span> <span data-ttu-id="a0cea-276">Assurez-vous que votre demande ne fait pas les deux.</span><span class="sxs-lookup"><span data-stu-id="a0cea-276">Ensure that your application does not do both.</span></span>

<a id="vs"></a>

## <a name="visual-studio-issues"></a><span data-ttu-id="a0cea-277">Problèmes de studio visuel</span><span class="sxs-lookup"><span data-stu-id="a0cea-277">Visual Studio issues</span></span>

<span data-ttu-id="a0cea-278">Cette section décrit les problèmes rencontrés dans Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="a0cea-278">This section describes issues encountered in Visual Studio.</span></span>

### <a name="script-documents-node-does-not-appear-in-solution-explorer"></a><span data-ttu-id="a0cea-279">Script Documents nœud n’apparaît pas dans Solution Explorer</span><span class="sxs-lookup"><span data-stu-id="a0cea-279">Script Documents node does not appear in Solution Explorer</span></span>

<span data-ttu-id="a0cea-280">Certains de nos tutoriels vous dirigent vers le nœud "Script Documents" dans Solution Explorer tout en débogage.</span><span class="sxs-lookup"><span data-stu-id="a0cea-280">Some of our tutorials direct you to the "Script Documents" node in Solution Explorer while debugging.</span></span> <span data-ttu-id="a0cea-281">Ce nœud est produit par le debugger JavaScript, et n’apparaîtra que lorsque vous débogage des clients navigateur dans Internet Explorer; le nœud n’apparaîtra pas si Chrome ou Firefox sont utilisés.</span><span class="sxs-lookup"><span data-stu-id="a0cea-281">This node is produced by the JavaScript debugger, and will only appear while debugging browser clients in Internet Explorer; the node will not appear if Chrome or Firefox are used.</span></span> <span data-ttu-id="a0cea-282">Le debugger JavaScript ne fonctionnera pas non plus si un autre debugger client est en cours d’exécution, comme le débbugger Silverlight.</span><span class="sxs-lookup"><span data-stu-id="a0cea-282">The JavaScript debugger will also not run if another client debugger is running, such as the Silverlight debugger.</span></span>

### <a name="signalr-does-not-work-on-visual-studio-2008-or-earlier"></a><span data-ttu-id="a0cea-283">SignalR ne fonctionne pas sur Visual Studio 2008 ou plus tôt</span><span class="sxs-lookup"><span data-stu-id="a0cea-283">SignalR does not work on Visual Studio 2008 or earlier</span></span>

<span data-ttu-id="a0cea-284">Ce comportement est normal.</span><span class="sxs-lookup"><span data-stu-id="a0cea-284">This behavior is by design.</span></span> <span data-ttu-id="a0cea-285">SignalR nécessite .NET Framework 4 ou plus tard; cela exige que les applications SignalR soient développées dans Visual Studio 2010 ou plus tard.</span><span class="sxs-lookup"><span data-stu-id="a0cea-285">SignalR requires .NET Framework 4 or later; this requires that SignalR applications be developed in Visual Studio 2010 or later.</span></span>

<a id="iis"></a>

## <a name="iis-issues"></a><span data-ttu-id="a0cea-286">Questions DE LAS</span><span class="sxs-lookup"><span data-stu-id="a0cea-286">IIS issues</span></span>

<span data-ttu-id="a0cea-287">Cette section contient des problèmes avec les services d’information Sur Internet.</span><span class="sxs-lookup"><span data-stu-id="a0cea-287">This section contains issues with Internet Information Services.</span></span>

### <a name="web-site-crashes-after-maphubs-call"></a><span data-ttu-id="a0cea-288">Le site Web s’écrase après l’appel de MapHubs</span><span class="sxs-lookup"><span data-stu-id="a0cea-288">Web site crashes after MapHubs call</span></span>

<span data-ttu-id="a0cea-289">Ce problème a été résolu dans la dernière version de SignalR.</span><span class="sxs-lookup"><span data-stu-id="a0cea-289">This issue has been fixed in the latest version of SignalR.</span></span> <span data-ttu-id="a0cea-290">Vérifiez que vous utilisez la dernière version de SignalR en mettant à jour votre installation à l’aide de NuGet.</span><span class="sxs-lookup"><span data-stu-id="a0cea-290">Verify that you are using the latest released version of SignalR by updating your installation using NuGet.</span></span>

<a id="azure"></a>

## <a name="azure-issues"></a><span data-ttu-id="a0cea-291">Questions Azure</span><span class="sxs-lookup"><span data-stu-id="a0cea-291">Azure issues</span></span>

<span data-ttu-id="a0cea-292">Cette section contient des problèmes avec Microsoft Azure.</span><span class="sxs-lookup"><span data-stu-id="a0cea-292">This section contains issues with Microsoft Azure.</span></span>

### <a name="messages-are-not-received-through-the-azure-backplane-after-altering-topic-names"></a><span data-ttu-id="a0cea-293">Les messages ne sont pas reçus par l’avion de fond Azure après avoir modifié les noms de sujet</span><span class="sxs-lookup"><span data-stu-id="a0cea-293">Messages are not received through the Azure backplane after altering topic names</span></span>

<span data-ttu-id="a0cea-294">Les sujets utilisés par l’avion de fond Azure ne sont pas destinés à être configurables par l’utilisateur.</span><span class="sxs-lookup"><span data-stu-id="a0cea-294">The topics used by the Azure backplane are not intended to be user-configurable.</span></span>
