---
uid: web-pages/overview/data/7-displaying-data-in-a-chart
title: Affichage des données dans un graphique avec ASP.NET pages Web (Razor) Microsoft Docs
author: rick-anderson
description: Ce chapitre explique comment afficher les données dans un graphique. Dans les chapitres précédents, vous avez appris à afficher les données manuellement et dans une grille. Ce chapitre explique...
ms.author: riande
ms.date: 05/22/2012
ms.assetid: f889fd46-4dac-4ecb-83d8-60e64c22036e
msc.legacyurl: /web-pages/overview/data/7-displaying-data-in-a-chart
msc.type: authoredcontent
ms.openlocfilehash: ad55d4898ee39e0239ef8b0bd5eea6387af3c816
ms.sourcegitcommit: 022f79dbc1350e0c6ffaa1e7e7c6e850cdabf9af
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 04/17/2020
ms.locfileid: "81542883"
---
# <a name="displaying-data-in-a-chart-with-aspnet-web-pages-razor"></a><span data-ttu-id="9cced-105">Affichage des données dans un graphique avec ASP.NET pages Web (Razor)</span><span class="sxs-lookup"><span data-stu-id="9cced-105">Displaying Data in a Chart with ASP.NET Web Pages (Razor)</span></span>

<span data-ttu-id="9cced-106">par [Microsoft](https://github.com/microsoft)</span><span class="sxs-lookup"><span data-stu-id="9cced-106">by [Microsoft](https://github.com/microsoft)</span></span>

> <span data-ttu-id="9cced-107">Cet article explique comment utiliser un graphique pour afficher des données dans un `Chart` site Web ASP.NET Pages Web (Razor) en utilisant l’aide.</span><span class="sxs-lookup"><span data-stu-id="9cced-107">This article explains how to use a chart to display data in an ASP.NET Web Pages (Razor) website by using the `Chart` helper.</span></span>
> 
> <span data-ttu-id="9cced-108">**Ce que vous apprendrez**:</span><span class="sxs-lookup"><span data-stu-id="9cced-108">**What you'll learn**:</span></span>
> 
> - <span data-ttu-id="9cced-109">Comment afficher les données dans un graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-109">How to display data in a chart.</span></span>
> - <span data-ttu-id="9cced-110">Comment style graphiques en utilisant des thèmes intégrés.</span><span class="sxs-lookup"><span data-stu-id="9cced-110">How to style charts using built-in themes.</span></span>
> - <span data-ttu-id="9cced-111">Comment enregistrer les graphiques et comment les mettre en cache pour de meilleures performances.</span><span class="sxs-lookup"><span data-stu-id="9cced-111">How to save charts and how to cache them for better performance.</span></span>
> 
> <span data-ttu-id="9cced-112">Voici les ASP.NET fonctionnalités de programmation introduites dans l’article :</span><span class="sxs-lookup"><span data-stu-id="9cced-112">These are the ASP.NET programming features introduced in the article:</span></span>
> 
> - <span data-ttu-id="9cced-113">L’aide. `Chart`</span><span class="sxs-lookup"><span data-stu-id="9cced-113">The `Chart` helper.</span></span>
> 
> > [!NOTE]
> > <span data-ttu-id="9cced-114">Les informations contenues dans cet article s’appliquent aux pages Web ASP.NET 1.0 et Web Pages 2.</span><span class="sxs-lookup"><span data-stu-id="9cced-114">The information in this article applies to ASP.NET Web Pages 1.0 and Web Pages 2.</span></span>

<a id="The_Chart_Helper"></a>
## <a name="the-chart-helper"></a><span data-ttu-id="9cced-115">L’aide graphique</span><span class="sxs-lookup"><span data-stu-id="9cced-115">The Chart Helper</span></span>

<span data-ttu-id="9cced-116">Lorsque vous souhaitez afficher vos données sous forme `Chart` graphique, vous pouvez utiliser l’aide.</span><span class="sxs-lookup"><span data-stu-id="9cced-116">When you want to display your data in graphical form, you can use `Chart` helper.</span></span> <span data-ttu-id="9cced-117">L’aide `Chart` peut rendre une image qui affiche des données dans une variété de types de graphiques.</span><span class="sxs-lookup"><span data-stu-id="9cced-117">The `Chart` helper can render an image that displays data in a variety of chart types.</span></span> <span data-ttu-id="9cced-118">Il prend en charge de nombreuses options pour le formatage et l’étiquetage.</span><span class="sxs-lookup"><span data-stu-id="9cced-118">It supports many options for formatting and labeling.</span></span> <span data-ttu-id="9cced-119">L’aide `Chart` peut rendre plus de 30 types de graphiques, y compris tous les types de graphiques que vous pourriez être familier avec de Microsoft Excel ou d’autres outils &#8212; graphiques de zone, graphiques à barres, graphiques de colonnes, graphiques en ligne, et graphiques à secteurs, ainsi que des graphiques plus spécialisés comme les graphiques boursiers.</span><span class="sxs-lookup"><span data-stu-id="9cced-119">The `Chart` helper can render more than 30 types of charts, including all the types of charts that you might be familiar with from Microsoft Excel or other tools &#8212; area charts, bar charts, column charts, line charts, and pie charts, along with more specialized charts like stock charts.</span></span>

| <span data-ttu-id="9cced-120">Description **du graphique** ![de zone : Image du type de graphique de secteur](7-displaying-data-in-a-chart/_static/image1.jpg)</span><span class="sxs-lookup"><span data-stu-id="9cced-120">**Area chart** ![Description: Picture of the Area chart type](7-displaying-data-in-a-chart/_static/image1.jpg)</span></span> | <span data-ttu-id="9cced-121">**Description du graphique** ![à barres : Image du type de graphique de barre](7-displaying-data-in-a-chart/_static/image2.jpg)</span><span class="sxs-lookup"><span data-stu-id="9cced-121">**Bar chart** ![Description: Picture of the Bar chart type](7-displaying-data-in-a-chart/_static/image2.jpg)</span></span> |
| --- | --- |
| <span data-ttu-id="9cced-122">**Description du graphique** ![de colonne : Image du type de graphique de colonne](7-displaying-data-in-a-chart/_static/image3.jpg)</span><span class="sxs-lookup"><span data-stu-id="9cced-122">**Column chart** ![Description: Picture of the Column chart type](7-displaying-data-in-a-chart/_static/image3.jpg)</span></span> | <span data-ttu-id="9cced-123">**Description du graphique** ![de ligne : Image du type de graphique de ligne](7-displaying-data-in-a-chart/_static/image4.jpg)</span><span class="sxs-lookup"><span data-stu-id="9cced-123">**Line chart** ![Description: Picture of the Line chart type](7-displaying-data-in-a-chart/_static/image4.jpg)</span></span> |
| <span data-ttu-id="9cced-124">**Description du graphique** ![à secteurs: Image du type de graphique à tarte](7-displaying-data-in-a-chart/_static/image5.jpg)</span><span class="sxs-lookup"><span data-stu-id="9cced-124">**Pie chart** ![Description: Picture of the Pie chart type](7-displaying-data-in-a-chart/_static/image5.jpg)</span></span> | <span data-ttu-id="9cced-125">**Description du graphique** ![boursier: Image du type stock chart](7-displaying-data-in-a-chart/_static/image6.jpg)</span><span class="sxs-lookup"><span data-stu-id="9cced-125">**Stock chart** ![Description: Picture of the Stock chart type](7-displaying-data-in-a-chart/_static/image6.jpg)</span></span> |

### <a name="chart-elements"></a><span data-ttu-id="9cced-126">Éléments de graphique</span><span class="sxs-lookup"><span data-stu-id="9cced-126">Chart Elements</span></span>

<span data-ttu-id="9cced-127">Les graphiques montrent des données et des éléments supplémentaires comme des légendes, des haches, des séries, etc.</span><span class="sxs-lookup"><span data-stu-id="9cced-127">Charts show data and additional elements like legends, axes, series, and so on.</span></span> <span data-ttu-id="9cced-128">L’image suivante montre de nombreux éléments graphiques que `Chart` vous pouvez personnaliser lorsque vous utilisez l’aide.</span><span class="sxs-lookup"><span data-stu-id="9cced-128">The following picture shows many of the chart elements that you can customize when you use the `Chart` helper.</span></span> <span data-ttu-id="9cced-129">Cet article vous montre comment définir certains (pas tous) de ces éléments.</span><span class="sxs-lookup"><span data-stu-id="9cced-129">This article shows you how to set some (not all) of these elements.</span></span>

![Description: Photo montrant les éléments du graphique](7-displaying-data-in-a-chart/_static/image7.jpg)

<a id="Creating_a_Chart"></a>
## <a name="creating-a-chart-from-data"></a><span data-ttu-id="9cced-131">Création d’un graphique à partir de données</span><span class="sxs-lookup"><span data-stu-id="9cced-131">Creating a Chart from Data</span></span>

<span data-ttu-id="9cced-132">Les données que vous affichez dans un graphique peuvent provener d’un tableau, des résultats retournés à partir d’une base de données ou des données qui se trouvent dans un fichier XML.</span><span class="sxs-lookup"><span data-stu-id="9cced-132">The data you display in a chart can be from an array, from the results returned from a database, or from data that's in an XML file.</span></span>

### <a name="using-an-array"></a><span data-ttu-id="9cced-133">Utilisation d’un tableau</span><span class="sxs-lookup"><span data-stu-id="9cced-133">Using an Array</span></span>

<span data-ttu-id="9cced-134">Comme expliqué dans [Introduction à ASP.NET Web Pages Programmation En utilisant la Syntaxe Razor](https://go.microsoft.com/fwlink/?LinkId=202890), un tableau vous permet de stocker une collection d’articles similaires dans une seule variable.</span><span class="sxs-lookup"><span data-stu-id="9cced-134">As explained in [Introduction to ASP.NET Web Pages Programming Using the Razor Syntax](https://go.microsoft.com/fwlink/?LinkId=202890), an array lets you store a collection of similar items in a single variable.</span></span> <span data-ttu-id="9cced-135">Vous pouvez utiliser des tableaux pour contenir les données que vous souhaitez inclure dans votre graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-135">You can use arrays to contain the data that you want to include in your chart.</span></span>

<span data-ttu-id="9cced-136">Cette procédure montre comment vous pouvez créer un graphique à partir de données dans les tableaux, en utilisant le type de graphique par défaut.</span><span class="sxs-lookup"><span data-stu-id="9cced-136">This procedure shows how you can create a chart from data in arrays, using the default chart type.</span></span> <span data-ttu-id="9cced-137">Il montre également comment afficher le graphique dans la page.</span><span class="sxs-lookup"><span data-stu-id="9cced-137">It also shows how to display the chart within the page.</span></span>

1. <span data-ttu-id="9cced-138">Créer un nouveau fichier nommé *ChartArrayBasic.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-138">Create a new file named *ChartArrayBasic.cshtml*.</span></span>
2. <span data-ttu-id="9cced-139">Remplacer le contenu existant par les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9cced-139">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample1.cshtml)]

    <span data-ttu-id="9cced-140">Le code crée d’abord un nouveau graphique et définit sa largeur et sa hauteur.</span><span class="sxs-lookup"><span data-stu-id="9cced-140">The code first creates a new chart and sets its width and height.</span></span> <span data-ttu-id="9cced-141">Vous spécifiez `AddTitle` le titre du graphique en utilisant la méthode.</span><span class="sxs-lookup"><span data-stu-id="9cced-141">You specify the chart title by using the `AddTitle` method.</span></span> <span data-ttu-id="9cced-142">Pour ajouter des données, vous utilisez la `AddSeries` méthode.</span><span class="sxs-lookup"><span data-stu-id="9cced-142">To add data, you use the `AddSeries` method.</span></span> <span data-ttu-id="9cced-143">Dans cet exemple, `name`vous `xValue`utilisez `yValues` le , `AddSeries` , et les paramètres de la méthode.</span><span class="sxs-lookup"><span data-stu-id="9cced-143">In this example, you use the `name`, `xValue`, and `yValues` parameters of the `AddSeries` method.</span></span> <span data-ttu-id="9cced-144">Le `name` paramètre est affiché dans la légende du graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-144">The `name` parameter is displayed in the chart legend.</span></span> <span data-ttu-id="9cced-145">Le `xValue` paramètre contient un tableau de données qui s’affiche le long de l’axe horizontal du graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-145">The `xValue` parameter contains an array of data that's displayed along the horizontal axis of the chart.</span></span> <span data-ttu-id="9cced-146">Le `yValues` paramètre contient un tableau de données qui est utilisé pour tracer les points verticaux du graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-146">The `yValues` parameter contains an array of data that's used to plot the vertical points of the chart.</span></span>

    <span data-ttu-id="9cced-147">La `Write` méthode rend en fait le graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-147">The `Write` method actually renders the chart.</span></span> <span data-ttu-id="9cced-148">Dans ce cas, parce que vous n’avez pas spécifier un type de graphique, l’aide `Chart` rend son graphique par défaut, qui est un graphique de colonne.</span><span class="sxs-lookup"><span data-stu-id="9cced-148">In this case, because you didn't specify a chart type, the `Chart` helper renders its default chart, which is a column chart.</span></span>
3. <span data-ttu-id="9cced-149">Exécutez la page dans le navigateur.</span><span class="sxs-lookup"><span data-stu-id="9cced-149">Run the page in the browser.</span></span> <span data-ttu-id="9cced-150">Le navigateur affiche le graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-150">The browser displays the chart.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image8.jpg)

### <a name="using-a-database-query-for-chart-data"></a><span data-ttu-id="9cced-151">Utilisation d’une requête de base de données pour les données graphiques</span><span class="sxs-lookup"><span data-stu-id="9cced-151">Using a Database Query for Chart Data</span></span>

<span data-ttu-id="9cced-152">Si les informations que vous souhaitez cartographier sont dans une base de données, vous pouvez exécuter une requête de base de données, puis utiliser les données des résultats pour créer le graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-152">If the information you want to chart is in a database, you can run a database query and then use data from the results to create the chart.</span></span> <span data-ttu-id="9cced-153">Cette procédure vous montre comment lire et afficher les données de la base de données créée dans [l’article Introduction à travailler avec une base de données dans ASP.NET sites de pages Web](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="9cced-153">This procedure shows you how to read and display the data from the database created in the article [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>

1. <span data-ttu-id="9cced-154">Ajoutez un dossier *App\_Data* à la racine du site Web si le dossier n’existe pas déjà.</span><span class="sxs-lookup"><span data-stu-id="9cced-154">Add an *App\_Data* folder to the root of the website if the folder does not already exist.</span></span>
2. <span data-ttu-id="9cced-155">Dans le dossier *App\_Data,* ajoutez le fichier de base de données nommé *SmallBakery.sdf* qui est décrit dans [Introduction to Working with a Database in ASP.NET Sites Web Pages](https://go.microsoft.com/fwlink/?LinkId=202893).</span><span class="sxs-lookup"><span data-stu-id="9cced-155">In the *App\_Data* folder, add the database file named *SmallBakery.sdf* that's described in [Introduction to Working with a Database in ASP.NET Web Pages Sites](https://go.microsoft.com/fwlink/?LinkId=202893).</span></span>
3. <span data-ttu-id="9cced-156">Créer un nouveau fichier nommé *ChartDataQuery.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-156">Create a new file named *ChartDataQuery.cshtml*.</span></span>
4. <span data-ttu-id="9cced-157">Remplacer le contenu existant par les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9cced-157">Replace the existing content with the following:</span></span>   

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample2.cshtml)]

    <span data-ttu-id="9cced-158">Le code ouvre d’abord la base de données `db`SmallBakery et l’attribue à une variable nommée .</span><span class="sxs-lookup"><span data-stu-id="9cced-158">The code first opens the SmallBakery database and assigns it to a variable named `db`.</span></span> <span data-ttu-id="9cced-159">Cette variable `Database` représente un objet qui peut être utilisé pour lire et écrire à la base de données.</span><span class="sxs-lookup"><span data-stu-id="9cced-159">This variable represents a `Database` object that can be used to read from and write to the database.</span></span> <span data-ttu-id="9cced-160">Ensuite, le code exécute une requête SQL pour obtenir le nom et le prix de chaque produit.</span><span class="sxs-lookup"><span data-stu-id="9cced-160">Next, the code runs a SQL query to get the name and price of each product.</span></span> <span data-ttu-id="9cced-161">Le code crée un nouveau graphique et passe la requête `DataBindTable` de base de données en appelant la méthode du graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-161">The code creates a new chart and passes the database query to it by calling the chart's `DataBindTable` method.</span></span> <span data-ttu-id="9cced-162">Cette méthode comporte deux `dataSource` paramètres : le paramètre est pour `xField` les données de la requête, et le paramètre vous permet de définir quelle colonne de données est utilisée pour l’axe x du graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-162">This method takes two parameters: the `dataSource` parameter is for the data from the query, and the `xField` parameter lets you set which data column is used for the chart's x-axis.</span></span>

    <span data-ttu-id="9cced-163">Comme alternative à `DataBindTable` l’utilisation de `AddSeries` la méthode, vous pouvez utiliser la méthode de l’aide. `Chart`</span><span class="sxs-lookup"><span data-stu-id="9cced-163">As an alternative to using the `DataBindTable` method, you can use the `AddSeries` method of the `Chart` helper.</span></span> <span data-ttu-id="9cced-164">La `AddSeries` méthode vous permet `xValue` `yValues` de définir les paramètres et les paramètres.</span><span class="sxs-lookup"><span data-stu-id="9cced-164">The `AddSeries` method lets you set the `xValue` and `yValues` parameters.</span></span> <span data-ttu-id="9cced-165">Par exemple, au `DataBindTable` lieu d’utiliser la méthode comme celle-ci:</span><span class="sxs-lookup"><span data-stu-id="9cced-165">For example, instead of using the `DataBindTable` method like this:</span></span>

    [!code-css[Main](7-displaying-data-in-a-chart/samples/sample3.css)]

    <span data-ttu-id="9cced-166">Vous pouvez `AddSeries` utiliser la méthode comme celle-ci:</span><span class="sxs-lookup"><span data-stu-id="9cced-166">You can use the `AddSeries` method like this:</span></span>

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample4.html)]

    <span data-ttu-id="9cced-167">Les deux rendent les mêmes résultats.</span><span class="sxs-lookup"><span data-stu-id="9cced-167">Both render the same results.</span></span> <span data-ttu-id="9cced-168">La `AddSeries` méthode est plus flexible car vous pouvez spécifier le type de graphique et les données plus explicitement, mais la `DataBindTable` méthode est plus facile à utiliser si vous n’avez pas besoin de flexibilité supplémentaire.</span><span class="sxs-lookup"><span data-stu-id="9cced-168">The `AddSeries` method is more flexible because you can specify the chart type and data more explicitly, but the `DataBindTable` method is easier to use if you don't need the extra flexibility.</span></span>
5. <span data-ttu-id="9cced-169">Exécutez la page dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="9cced-169">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image9.jpg)

### <a name="using-xml-data"></a><span data-ttu-id="9cced-170">Utilisation de données XML</span><span class="sxs-lookup"><span data-stu-id="9cced-170">Using XML Data</span></span>

<span data-ttu-id="9cced-171">La troisième option pour la cartographie est d’utiliser un fichier XML comme données pour le graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-171">The third option for charting is to use an XML file as the data for the chart.</span></span> <span data-ttu-id="9cced-172">Cela exige que le fichier XML dispose également d’un fichier schéma *(fichier .xsd)* qui décrit la structure XML.</span><span class="sxs-lookup"><span data-stu-id="9cced-172">This requires that the XML file also have a schema file (*.xsd* file) that describes the XML structure.</span></span> <span data-ttu-id="9cced-173">Cette procédure vous montre comment lire les données d’un fichier XML.</span><span class="sxs-lookup"><span data-stu-id="9cced-173">This procedure shows you how to read data from an XML file.</span></span>

1. <span data-ttu-id="9cced-174">Dans le dossier *App\_Data,* créez un nouveau fichier XML nommé *data.xml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-174">In the *App\_Data* folder, create a new XML file named *data.xml*.</span></span>
2. <span data-ttu-id="9cced-175">Remplacez le XML existant par ce qui suit, qui est quelques données XML sur les employés dans une entreprise fictive.</span><span class="sxs-lookup"><span data-stu-id="9cced-175">Replace the existing XML with the following, which is some XML data about employees in a fictional company.</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample5.xml)]
3. <span data-ttu-id="9cced-176">Dans le dossier *\_App Data,* créez un nouveau fichier XML nommé *data.xsd*.</span><span class="sxs-lookup"><span data-stu-id="9cced-176">In the *App\_Data* folder, create a new XML file named *data.xsd*.</span></span> <span data-ttu-id="9cced-177">(Notez que la prolongation de cette période est *.xsd*.)</span><span class="sxs-lookup"><span data-stu-id="9cced-177">(Note that the extension this time is *.xsd*.)</span></span>
4. <span data-ttu-id="9cced-178">Remplacer le XML existant par les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9cced-178">Replace the existing XML with the following:</span></span> 

    [!code-xml[Main](7-displaying-data-in-a-chart/samples/sample6.xml)]
5. <span data-ttu-id="9cced-179">Dans la racine du site, créer un nouveau fichier nommé *ChartDataXML.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-179">In the root of the website, create a new file named *ChartDataXML.cshtml*.</span></span>
6. <span data-ttu-id="9cced-180">Remplacer le contenu existant par les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9cced-180">Replace the existing content with the following:</span></span> 

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample7.cshtml)]

    <span data-ttu-id="9cced-181">Le code crée `DataSet` d’abord un objet.</span><span class="sxs-lookup"><span data-stu-id="9cced-181">The code first creates a `DataSet` object.</span></span> <span data-ttu-id="9cced-182">Cet objet est utilisé pour gérer les données qui sont lues à partir du fichier XML et l’organiser en fonction des informations contenues dans le fichier schéma.</span><span class="sxs-lookup"><span data-stu-id="9cced-182">This object is used to manage the data that's read from the XML file and organize it according to the information in the schema file.</span></span> <span data-ttu-id="9cced-183">(Notez que le haut du `using SystemData`code comprend l’instruction .</span><span class="sxs-lookup"><span data-stu-id="9cced-183">(Notice that the top of the code includes the statement `using SystemData`.</span></span> <span data-ttu-id="9cced-184">Cela est nécessaire pour pouvoir travailler `DataSet` avec l’objet.</span><span class="sxs-lookup"><span data-stu-id="9cced-184">This is required in order to be able to work with the `DataSet` object.</span></span> <span data-ttu-id="9cced-185">Pour plus d’informations, voir [ &quot;Utilisation des&quot; déclarations et des noms entièrement qualifiés](#SB_UsingStatements) plus tard dans cet article.)</span><span class="sxs-lookup"><span data-stu-id="9cced-185">For more information, see [&quot;Using&quot; Statements and Fully Qualified Names](#SB_UsingStatements) later in this article.)</span></span>

    <span data-ttu-id="9cced-186">Ensuite, le code `DataView` crée un objet basé sur le jeu de données.</span><span class="sxs-lookup"><span data-stu-id="9cced-186">Next, the code creates a `DataView` object based on the dataset.</span></span> <span data-ttu-id="9cced-187">La vue des données fournit un objet que le graphique peut lier à &#8212; qui est, lire et tracer.</span><span class="sxs-lookup"><span data-stu-id="9cced-187">The data view provides an object that the chart can bind to &#8212; that is, read and plot.</span></span> <span data-ttu-id="9cced-188">Le graphique se lie aux `AddSeries` données à l’aide de la méthode, comme `xValue` vous `yValues` l’avez vu `DataView` plus tôt lors de la cartographie des données de tableau, sauf que cette fois le et les paramètres sont réglés à l’objet.</span><span class="sxs-lookup"><span data-stu-id="9cced-188">The chart binds to the data using the `AddSeries` method, as you saw earlier when charting the array data, except that this time the `xValue` and `yValues` parameters are set to the `DataView` object.</span></span>

    <span data-ttu-id="9cced-189">Cet exemple vous montre également comment spécifier un type de graphique particulier.</span><span class="sxs-lookup"><span data-stu-id="9cced-189">This example also shows you how to specify a particular chart type.</span></span> <span data-ttu-id="9cced-190">Lorsque les données sont `AddSeries` ajoutées `chartType` dans la méthode, le paramètre est également défini pour afficher un graphique à secteurs.</span><span class="sxs-lookup"><span data-stu-id="9cced-190">When the data is added in the `AddSeries` method, the `chartType` parameter is also set to display a pie chart.</span></span>
7. <span data-ttu-id="9cced-191">Exécutez la page dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="9cced-191">Run the page in a browser.</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image10.jpg)

> [!TIP]
> 
> <a id="SB_UsingStatements"></a>
> ### <a name="using-statements-and-fully-qualified-names"></a><span data-ttu-id="9cced-192">Déclarations « utilisations » et noms entièrement qualifiés</span><span class="sxs-lookup"><span data-stu-id="9cced-192">"Using" Statements and Fully Qualified Names</span></span>
> 
> <span data-ttu-id="9cced-193">Le cadre .NET qui ASP.NET pages Web avec la syntaxe Razor est basé sur plusieurs milliers de composants (classes).</span><span class="sxs-lookup"><span data-stu-id="9cced-193">The .NET Framework that ASP.NET Web Pages with Razor syntax is based on consists of many thousands of components (classes).</span></span> <span data-ttu-id="9cced-194">Pour rendre gérable de travailler avec toutes ces classes, ils sont organisés en *espaces de noms*, qui sont un peu comme les bibliothèques.</span><span class="sxs-lookup"><span data-stu-id="9cced-194">To make it manageable to work with all these classes, they're organized into *namespaces*, which are somewhat like libraries.</span></span> <span data-ttu-id="9cced-195">Par exemple, `System.Web` l’espace nom contient des classes `System.Xml` qui prennent en charge la communication par navigateur/serveur, l’espace nom contient des classes qui sont utilisées pour créer et lire des fichiers XML, et l’espace `System.Data` nom contient des classes qui vous permettent de travailler avec des données.</span><span class="sxs-lookup"><span data-stu-id="9cced-195">For example, the `System.Web` namespace contains classes that support browser/server communication, the `System.Xml` namespace contains classes that are used to create and read XML files, and the `System.Data` namespace contains classes that let you work with data.</span></span>
> 
> <span data-ttu-id="9cced-196">Afin d’accéder à n’importe quelle classe donnée dans le cadre .NET, le code doit connaître non seulement le nom de classe, mais aussi l’espace de nom que la classe est en.</span><span class="sxs-lookup"><span data-stu-id="9cced-196">In order to access any given class in the .NET Framework, code needs to know not just the class name, but also the namespace that the class is in.</span></span> <span data-ttu-id="9cced-197">Par exemple, afin d’utiliser `Chart` l’aide, `System.Web.Helpers.Chart` le code doit trouver`System.Web.Helpers`la classe, qui`Chart`combine l’espace de nom ( ) avec le nom de classe ( ).</span><span class="sxs-lookup"><span data-stu-id="9cced-197">For example, in order to use the `Chart` helper, code needs to find the `System.Web.Helpers.Chart` class, which combines the namespace (`System.Web.Helpers`) with the class name (`Chart`).</span></span> <span data-ttu-id="9cced-198">C’est ce qu’on appelle le nom *entièrement qualifié* de la classe &#8212; son emplacement complet et sans ambiguïté dans l’immensité du cadre .NET.</span><span class="sxs-lookup"><span data-stu-id="9cced-198">This is known as the class's *fully-qualified* name &#8212; its complete, unambiguous location within the vastness of the .NET Framework.</span></span> <span data-ttu-id="9cced-199">Dans le code, cela ressemblerait à ce qui suit:</span><span class="sxs-lookup"><span data-stu-id="9cced-199">In code, this would look like the following:</span></span>
> 
> `var myChart = new System.Web.Helpers.Chart(width: 600, height: 400) // etc.`
> 
> <span data-ttu-id="9cced-200">Cependant, il est lourd (et sujet aux erreurs) d’avoir à utiliser ces longs noms entièrement qualifiés chaque fois que vous voulez vous référer à une classe ou un aide.</span><span class="sxs-lookup"><span data-stu-id="9cced-200">However, it's cumbersome (and error prone) to have to use these long, fully-qualified names every time you want to refer to a class or helper.</span></span> <span data-ttu-id="9cced-201">Par conséquent, pour le rendre plus facile à utiliser les noms de classe, vous pouvez *importer* les espaces de nom qui vous intéressent, qui est généralement juste une poignée parmi les nombreux espaces de noms dans le cadre .NET.</span><span class="sxs-lookup"><span data-stu-id="9cced-201">Therefore, to make it easier to use class names, you can *import* the namespaces you're interested in, which is usually is just a handful from among the many namespaces in the .NET Framework.</span></span> <span data-ttu-id="9cced-202">Si vous avez importé un namespace, vous pouvez`Chart`utiliser juste un nom`System.Web.Helpers.Chart`de classe ( ) au lieu du nom entièrement qualifié ().</span><span class="sxs-lookup"><span data-stu-id="9cced-202">If you've imported a namespace, you can use just a class name (`Chart`) instead of the fully qualified name (`System.Web.Helpers.Chart`).</span></span> <span data-ttu-id="9cced-203">Lorsque votre code s’exécute et rencontre un nom de classe, il peut regarder dans les espaces de nom que vous avez importés pour trouver cette classe.</span><span class="sxs-lookup"><span data-stu-id="9cced-203">When your code runs and encounters a class name, it can look in just the namespaces you've imported to find that class.</span></span>
> 
> <span data-ttu-id="9cced-204">Lorsque vous utilisez ASP.NET Pages Web avec la syntaxe Razor pour créer des pages `WebPage` Web, vous utilisez généralement le même ensemble de classes à chaque fois, y compris la classe, les différents aides, et ainsi de suite.</span><span class="sxs-lookup"><span data-stu-id="9cced-204">When you use ASP.NET Web Pages with Razor syntax to create web pages, you typically use the same set of classes each time, including the `WebPage` class, the various helpers, and so on.</span></span> <span data-ttu-id="9cced-205">Pour vous éviter le travail d’importation des espaces de noms pertinents chaque fois que vous créez un site Web, ASP.NET est configuré de sorte qu’il importe automatiquement un ensemble d’espaces de noms de base pour chaque site Web.</span><span class="sxs-lookup"><span data-stu-id="9cced-205">To save you the work of importing the relevant namespaces every time you create a website, ASP.NET is configured so it automatically imports a set of core namespaces for every website.</span></span> <span data-ttu-id="9cced-206">C’est pourquoi vous n’avez pas eu à faire face à des espaces de nom ou d’importer jusqu’à présent; toutes les classes avec lesquelles vous avez travaillé sont dans des espaces de nom qui sont déjà importés pour vous.</span><span class="sxs-lookup"><span data-stu-id="9cced-206">That's why you haven't had to deal with namespaces or importing up to now; all the classes you've worked with are in namespaces that are already imported for you.</span></span>
> 
> <span data-ttu-id="9cced-207">Cependant, parfois, vous devez travailler avec une classe qui n’est pas dans un espace de nom qui est automatiquement importé pour vous.</span><span class="sxs-lookup"><span data-stu-id="9cced-207">However, sometimes you need to work with a class that isn't in a namespace that's automatically imported for you.</span></span> <span data-ttu-id="9cced-208">Dans ce cas, vous pouvez soit utiliser le nom entièrement qualifié de cette classe, soit vous pouvez importer manuellement l’espace nom qui contient la classe.</span><span class="sxs-lookup"><span data-stu-id="9cced-208">In that case, you can either use that class's fully-qualified name, or you can manually import the namespace that contains the class.</span></span> <span data-ttu-id="9cced-209">Pour importer un espace nomin, vous utilisez la `using` déclaration (dans`import` Visual Basic), comme vous l’avez vu dans un exemple plus tôt l’article.</span><span class="sxs-lookup"><span data-stu-id="9cced-209">To import a namespace, you use the `using` statement (`import` in Visual Basic), as you saw in an example earlier the article.</span></span>
> 
> <span data-ttu-id="9cced-210">Par exemple, `DataSet` la classe `System.Data` est dans l’espace nom.</span><span class="sxs-lookup"><span data-stu-id="9cced-210">For example, the `DataSet` class is in the `System.Data` namespace.</span></span> <span data-ttu-id="9cced-211">L’espace `System.Data` nom n’est pas automatiquement disponible pour ASP.NET pages Razor.</span><span class="sxs-lookup"><span data-stu-id="9cced-211">The `System.Data` namespace is not automatically available to ASP.NET Razor pages.</span></span> <span data-ttu-id="9cced-212">Par conséquent, pour `DataSet` travailler avec la classe en utilisant son nom entièrement qualifié, vous pouvez utiliser le code comme ceci:</span><span class="sxs-lookup"><span data-stu-id="9cced-212">Therefore, to work with the `DataSet` class using its fully qualified name, you can use code like this:</span></span>
> 
> `var dataSet = new System.Data.DataSet();`
> 
> <span data-ttu-id="9cced-213">Si vous devez `DataSet` utiliser la classe à plusieurs reprises, vous pouvez importer un espace de nom comme celui-ci et ensuite utiliser le nom de classe dans le code:</span><span class="sxs-lookup"><span data-stu-id="9cced-213">If you have to use the `DataSet` class repeatedly you can import a namespace like this and then use just the class name in code:</span></span>
> 
> [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample8.cshtml)]
> 
> <span data-ttu-id="9cced-214">Vous pouvez `using` ajouter des instructions pour tous les autres espaces nom de cadre .NET que vous souhaitez référencer.</span><span class="sxs-lookup"><span data-stu-id="9cced-214">You can add `using` statements for any other .NET Framework namespaces that you want to reference.</span></span> <span data-ttu-id="9cced-215">Cependant, comme indiqué, vous n’aurez pas besoin de le faire souvent, parce que la plupart des classes que vous travaillerez avec sont dans les espaces de nom qui sont importés automatiquement par ASP.NET pour une utilisation dans *.cshtml* et *.vbhtml* pages.</span><span class="sxs-lookup"><span data-stu-id="9cced-215">However, as noted, you won't need to do this often, because most of the classes that you'll work with are in namespaces that are imported automatically by ASP.NET for use in *.cshtml* and *.vbhtml* pages.</span></span>

<a id="Displaying_Charts"></a>
## <a name="displaying-charts-inside-a-web-page"></a><span data-ttu-id="9cced-216">Affichage de graphiques à l’intérieur d’une page Web</span><span class="sxs-lookup"><span data-stu-id="9cced-216">Displaying Charts Inside a Web Page</span></span>

<span data-ttu-id="9cced-217">Dans les exemples que vous avez vus jusqu’à présent, vous créez un graphique, puis le graphique est rendu directement au navigateur comme un graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-217">In the examples you've seen so far, you create a chart and then the chart is rendered directly to the browser as a graphic.</span></span> <span data-ttu-id="9cced-218">Dans de nombreux cas, cependant, vous voulez afficher un graphique dans le cadre d’une page, pas seulement par lui-même dans le navigateur.</span><span class="sxs-lookup"><span data-stu-id="9cced-218">In many cases, though, you want to display a chart as part of a page, not just by itself in the browser.</span></span> <span data-ttu-id="9cced-219">Pour ce faire, il faut un processus en deux étapes.</span><span class="sxs-lookup"><span data-stu-id="9cced-219">To do that requires a two-step process.</span></span> <span data-ttu-id="9cced-220">La première étape consiste à créer une page qui génère le graphique, comme vous l’avez déjà vu.</span><span class="sxs-lookup"><span data-stu-id="9cced-220">The first step is to create a page that generates the chart, as you've already seen.</span></span>

<span data-ttu-id="9cced-221">La deuxième étape consiste à afficher l’image résultante dans une autre page.</span><span class="sxs-lookup"><span data-stu-id="9cced-221">The second step is to display the resulting image in another page.</span></span> <span data-ttu-id="9cced-222">Pour afficher l’image, `<img>` vous utilisez un élément HTML, de la même manière que vous pouvez afficher n’importe quelle image.</span><span class="sxs-lookup"><span data-stu-id="9cced-222">To display the image, you use an HTML `<img>` element, in the same way you would to display any image.</span></span> <span data-ttu-id="9cced-223">Cependant, au lieu de faire référence à un `<img>` fichier *.jpg* ou *.png,* l’élément fait référence au fichier *.cshtml* qui contient l’aide `Chart` qui crée le graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-223">However, instead of referencing a *.jpg* or *.png* file, the `<img>` element references the *.cshtml* file that contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="9cced-224">Lorsque la page d’affichage s’exécute, l’élément `<img>` obtient la sortie de l’aide `Chart` et rend le graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-224">When the display page runs, the `<img>` element gets the output of the `Chart` helper and renders the chart.</span></span>

![](7-displaying-data-in-a-chart/_static/image11.jpg)

1. <span data-ttu-id="9cced-225">Créer un fichier nommé *ShowChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-225">Create a file named *ShowChart.cshtml*.</span></span>
2. <span data-ttu-id="9cced-226">Remplacer le contenu existant par les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9cced-226">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample9.html)]

    <span data-ttu-id="9cced-227">Le code `<img>` utilise l’élément pour afficher le graphique que vous avez créé plus tôt dans le fichier *ChartArrayBasic.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="9cced-227">The code uses the `<img>` element to display the chart that you created earlier in the *ChartArrayBasic.cshtml* file.</span></span>
3. <span data-ttu-id="9cced-228">Exécutez la page Web dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="9cced-228">Run the web page in a browser.</span></span> <span data-ttu-id="9cced-229">Le fichier *ShowChart.cshtml* affiche l’image graphique basée sur le code contenu dans le fichier *ChartArrayBasic.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="9cced-229">The *ShowChart.cshtml* file displays the chart image based on the code contained in the *ChartArrayBasic.cshtml* file.</span></span>

<a id="Styling_a_Chart"></a>
## <a name="styling-a-chart"></a><span data-ttu-id="9cced-230">Styling a Chart</span><span class="sxs-lookup"><span data-stu-id="9cced-230">Styling a Chart</span></span>

<span data-ttu-id="9cced-231">L’aide `Chart` prend en charge un grand nombre d’options qui vous permettent de personnaliser l’apparence du graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-231">The `Chart` helper supports a large number of options that let you customize the appearance of the chart.</span></span> <span data-ttu-id="9cced-232">Vous pouvez définir des couleurs, des polices, des bordures, et ainsi de suite.</span><span class="sxs-lookup"><span data-stu-id="9cced-232">You can set colors, fonts, borders, and so on.</span></span> <span data-ttu-id="9cced-233">Un moyen facile de personnaliser l’apparence d’un graphique est d’utiliser un *thème*.</span><span class="sxs-lookup"><span data-stu-id="9cced-233">An easy way to customize the appearance of a chart is to use a *theme*.</span></span> <span data-ttu-id="9cced-234">Les thèmes sont des collections d'informations qui spécifient de quelle manière restituer un graphique à l'aide de couleurs, d'étiquettes, de palettes, de bordures et d'effets.</span><span class="sxs-lookup"><span data-stu-id="9cced-234">Themes are collections of information that specify how to render a chart using fonts, colors, labels, palettes, borders, and effects.</span></span> <span data-ttu-id="9cced-235">(Notez que le style d’un graphique n’indique pas le type de graphique.)</span><span class="sxs-lookup"><span data-stu-id="9cced-235">(Note that the style of a chart does not indicate the type of chart.)</span></span>

<span data-ttu-id="9cced-236">Le tableau suivant répertorie les thèmes intégrés.</span><span class="sxs-lookup"><span data-stu-id="9cced-236">The following table lists built-in themes.</span></span>

| <span data-ttu-id="9cced-237">Thème</span><span class="sxs-lookup"><span data-stu-id="9cced-237">Theme</span></span> | <span data-ttu-id="9cced-238">Description</span><span class="sxs-lookup"><span data-stu-id="9cced-238">Description</span></span> |
| --- | --- |
| `Vanilla` | <span data-ttu-id="9cced-239">Affiche des colonnes rouges sur un fond blanc.</span><span class="sxs-lookup"><span data-stu-id="9cced-239">Displays red columns on a white background.</span></span> |
| `Blue` | <span data-ttu-id="9cced-240">Affiche des colonnes bleues sur un fond de gradient bleu.</span><span class="sxs-lookup"><span data-stu-id="9cced-240">Displays blue columns on a blue gradient background.</span></span> |
| `Green` | <span data-ttu-id="9cced-241">Affiche des colonnes bleues sur un fond de gradient vert.</span><span class="sxs-lookup"><span data-stu-id="9cced-241">Displays blue columns on a green gradient background.</span></span> |
| `Yellow` | <span data-ttu-id="9cced-242">Affiche des colonnes oranges sur un fond de gradient jaune.</span><span class="sxs-lookup"><span data-stu-id="9cced-242">Displays orange columns on a yellow gradient background.</span></span> |
| `Vanilla3D` | <span data-ttu-id="9cced-243">Affiche des colonnes rouges 3D sur un fond blanc.</span><span class="sxs-lookup"><span data-stu-id="9cced-243">Displays 3-D red columns on a white background.</span></span> |

<span data-ttu-id="9cced-244">Vous pouvez spécifier le thème à utiliser lorsque vous créez un nouveau graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-244">You can specify the theme to use when you create a new chart.</span></span>

1. <span data-ttu-id="9cced-245">Créez un nouveau fichier nommé *ChartStyleGreen.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-245">Create a new file named *ChartStyleGreen.cshtml*.</span></span>
2. <span data-ttu-id="9cced-246">Remplacer le contenu existant de la page par les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9cced-246">Replace the existing content in the page with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample10.cshtml)]

    <span data-ttu-id="9cced-247">Ce code est le même que l’exemple précédent qui `theme` utilise la `Chart` base de données pour les données, mais ajoute le paramètre quand il crée l’objet.</span><span class="sxs-lookup"><span data-stu-id="9cced-247">This code is the same as the earlier example that uses the database for data, but adds the `theme` parameter when it creates the `Chart` object.</span></span> <span data-ttu-id="9cced-248">Ce qui suit montre le code modifié :</span><span class="sxs-lookup"><span data-stu-id="9cced-248">The following shows the changed code:</span></span>

    [!code-csharp[Main](7-displaying-data-in-a-chart/samples/sample11.cs)]
3. <span data-ttu-id="9cced-249">Exécutez la page dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="9cced-249">Run the page in a browser.</span></span> <span data-ttu-id="9cced-250">Vous voyez les mêmes données qu’auparavant, mais le graphique semble plus poli:</span><span class="sxs-lookup"><span data-stu-id="9cced-250">You see the same data as before, but the chart looks more polished:</span></span> 

    ![](7-displaying-data-in-a-chart/_static/image12.jpg)

<a id="Saving_a_Chart"></a>
## <a name="saving-a-chart"></a><span data-ttu-id="9cced-251">Enregistrer un graphique</span><span class="sxs-lookup"><span data-stu-id="9cced-251">Saving a Chart</span></span>

<span data-ttu-id="9cced-252">Lorsque vous `Chart` utilisez l’aide comme vous l’avez vu jusqu’à présent dans cet article, l’aide re-crée le graphique à partir de zéro chaque fois qu’il est invoqué.</span><span class="sxs-lookup"><span data-stu-id="9cced-252">When you use the `Chart` helper as you've seen so far in this article, the helper re-creates the chart from scratch each time it's invoked.</span></span> <span data-ttu-id="9cced-253">Si nécessaire, le code du graphique ré-interroge également la base de données ou relit le fichier XML pour obtenir les données.</span><span class="sxs-lookup"><span data-stu-id="9cced-253">If necessary, the code for the chart also re-queries the database or re-reads the XML file to get the data.</span></span> <span data-ttu-id="9cced-254">Dans certains cas, cela peut être une opération complexe, comme si la base de données que vous interrogez est grande, ou si le fichier XML contient beaucoup de données.</span><span class="sxs-lookup"><span data-stu-id="9cced-254">In some cases, doing this can be a complex operation, such as if the database that you're querying is large, or if the XML file contains a lot of data.</span></span> <span data-ttu-id="9cced-255">Même si le graphique n’implique pas beaucoup de données, le processus de création dynamique d’une image prend des ressources serveur, et si de nombreuses personnes demandent la page ou les pages qui affichent le graphique, il peut y avoir un impact sur les performances de votre site Web.</span><span class="sxs-lookup"><span data-stu-id="9cced-255">Even if the chart doesn't involve a lot of data, the process of dynamically creating an image takes up server resources, and if many people request the page or pages that display the chart, there can be an impact on the performance of your website.</span></span>

<span data-ttu-id="9cced-256">Pour vous aider à réduire l’impact potentiel de la création d’un graphique, vous pouvez créer un graphique la première fois que vous en avez besoin, puis l’enregistrer.</span><span class="sxs-lookup"><span data-stu-id="9cced-256">To help you reduce the potential performance impact of creating a chart, you can create a chart the first time you need it and then save it.</span></span> <span data-ttu-id="9cced-257">Lorsque le graphique est nécessaire à nouveau, plutôt que de le régénérer, vous pouvez simplement aller chercher la version enregistrée et le rendre.</span><span class="sxs-lookup"><span data-stu-id="9cced-257">When the chart is needed again, rather than regenerating it, you can just fetch the saved version and render that.</span></span>

<span data-ttu-id="9cced-258">Vous pouvez enregistrer un graphique de ces façons :</span><span class="sxs-lookup"><span data-stu-id="9cced-258">You can save a chart in these ways:</span></span>

- <span data-ttu-id="9cced-259">Cachez le graphique dans la mémoire de l’ordinateur (sur le serveur).</span><span class="sxs-lookup"><span data-stu-id="9cced-259">Cache the chart in computer memory (on the server).</span></span>
- <span data-ttu-id="9cced-260">Enregistrez le graphique sous forme de fichier d’image.</span><span class="sxs-lookup"><span data-stu-id="9cced-260">Save the chart as an image file.</span></span>
- <span data-ttu-id="9cced-261">Enregistrez le graphique sous forme de fichier XML.</span><span class="sxs-lookup"><span data-stu-id="9cced-261">Save the chart as an XML file.</span></span> <span data-ttu-id="9cced-262">Cette option vous permet de modifier le graphique avant de l’enregistrer.</span><span class="sxs-lookup"><span data-stu-id="9cced-262">This option lets you modify the chart before you save it.</span></span>

### <a name="caching-a-chart"></a><span data-ttu-id="9cced-263">Caching a Chart</span><span class="sxs-lookup"><span data-stu-id="9cced-263">Caching a Chart</span></span>

<span data-ttu-id="9cced-264">Une fois que vous avez créé un graphique, vous pouvez le mettre en cache.</span><span class="sxs-lookup"><span data-stu-id="9cced-264">After you've created a chart, you can cache it.</span></span> <span data-ttu-id="9cced-265">Le cachement d’un graphique signifie qu’il n’a pas besoin d’être recréé s’il doit être affiché à nouveau.</span><span class="sxs-lookup"><span data-stu-id="9cced-265">Caching a chart means that it doesn't have to be re-created if it needs to be displayed again.</span></span> <span data-ttu-id="9cced-266">Lorsque vous enregistrez un graphique dans le cache, vous lui donnez une clé qui doit être unique à ce graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-266">When you save a chart in the cache, you give it a key that must be unique to that chart.</span></span>

<span data-ttu-id="9cced-267">Les graphiques enregistrés sur le cache peuvent être supprimés si le serveur est faible sur la mémoire.</span><span class="sxs-lookup"><span data-stu-id="9cced-267">Charts saved to the cache might be removed if the server runs low on memory.</span></span> <span data-ttu-id="9cced-268">En outre, le cache est effacé si votre application redémarre pour une raison quelconque.</span><span class="sxs-lookup"><span data-stu-id="9cced-268">In addition, the cache is cleared if your application restarts for any reason.</span></span> <span data-ttu-id="9cced-269">Par conséquent, la façon standard de travailler avec un graphique en cache est de toujours vérifier d’abord si elle est disponible dans le cache, et si ce n’est pas le cas, puis de le créer ou de le recréer.</span><span class="sxs-lookup"><span data-stu-id="9cced-269">Therefore, the standard way to work with a cached chart is to always check first whether it's available in the cache, and if not, then to create or re-create it.</span></span>

1. <span data-ttu-id="9cced-270">À la racine de votre site Web, créez un fichier nommé *ShowCachedChart.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-270">At the root of your website, create a file named *ShowCachedChart.cshtml*.</span></span>
2. <span data-ttu-id="9cced-271">Remplacer le contenu existant par les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9cced-271">Replace the existing content with the following:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample12.html)]

    <span data-ttu-id="9cced-272">L’étiquette `<img>` `src` comprend un attribut qui pointe vers le fichier *ChartSaveToCache.cshtml* et passe une clé à la page comme une chaîne de requête.</span><span class="sxs-lookup"><span data-stu-id="9cced-272">The `<img>` tag includes a `src` attribute that points to the *ChartSaveToCache.cshtml* file and passes a key to the page as a query string.</span></span> <span data-ttu-id="9cced-273">La clé contient &quot;la valeur&quot;myChartKey .</span><span class="sxs-lookup"><span data-stu-id="9cced-273">The key contains the value &quot;myChartKey&quot;.</span></span> <span data-ttu-id="9cced-274">Le fichier *ChartSaveToCache.cshtml* contient l’aide `Chart` qui crée le graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-274">The *ChartSaveToCache.cshtml* file contains the `Chart` helper that creates the chart.</span></span> <span data-ttu-id="9cced-275">Vous allez créer cette page dans un instant.</span><span class="sxs-lookup"><span data-stu-id="9cced-275">You'll create this page in a moment.</span></span>

    <span data-ttu-id="9cced-276">À la fin de la page il ya un lien vers une page nommée *ClearCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-276">At the end of the page there's a link to a page named *ClearCache.cshtml*.</span></span> <span data-ttu-id="9cced-277">C’est une page que vous allez également créer sous peu.</span><span class="sxs-lookup"><span data-stu-id="9cced-277">That's a page that you'll also create shortly.</span></span> <span data-ttu-id="9cced-278">Vous avez besoin de la *ClearCache.cshtml* seulement pour tester la mise en cache pour cet exemple - ce n’est pas un lien ou une page que vous incluriez normalement lorsque vous travaillez avec des graphiques en cache.</span><span class="sxs-lookup"><span data-stu-id="9cced-278">You need the *ClearCache.cshtml* only to test caching for this example — it's not a link or page that you'd normally include when working with cached charts.</span></span>
3. <span data-ttu-id="9cced-279">À la racine de votre site Web, créez un nouveau fichier nommé *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-279">At the root of your website, create a new file named *ChartSaveToCache.cshtml*.</span></span>
4. <span data-ttu-id="9cced-280">Remplacer le contenu existant par les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9cced-280">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample13.cshtml)]

    <span data-ttu-id="9cced-281">Le code vérifie d’abord si quelque chose a été adopté comme la valeur clé dans la chaîne de requête.</span><span class="sxs-lookup"><span data-stu-id="9cced-281">The code first checks whether anything was passed as the key value in the query string.</span></span> <span data-ttu-id="9cced-282">Si c’est le cas, le code essaie `GetFromCache` de lire un graphique hors du cache en appelant la méthode et en la passant la clé.</span><span class="sxs-lookup"><span data-stu-id="9cced-282">If so, the code tries to read a chart out of the cache by calling the `GetFromCache` method and passing it the key.</span></span> <span data-ttu-id="9cced-283">S’il s’avère qu’il n’y a rien dans le cache sous cette clé (ce qui se produirait la première fois que le graphique est demandé), le code crée le graphique comme d’habitude.</span><span class="sxs-lookup"><span data-stu-id="9cced-283">If it turns out that there's nothing in the cache under that key (which would happen the first time that the chart is requested), the code creates the chart as usual.</span></span> <span data-ttu-id="9cced-284">Lorsque le graphique est terminé, le code `SaveToCache`l’enregistre dans le cache en appelant .</span><span class="sxs-lookup"><span data-stu-id="9cced-284">When the chart is finished, the code saves it to the cache by calling `SaveToCache`.</span></span> <span data-ttu-id="9cced-285">Cette méthode nécessite une clé (de sorte que le graphique peut être demandé plus tard), et la quantité de temps que le graphique doit être enregistré dans le cache.</span><span class="sxs-lookup"><span data-stu-id="9cced-285">That method requires a key (so the chart can be requested later), and the amount of time that the chart should be saved in the cache.</span></span> <span data-ttu-id="9cced-286">(L’heure exacte à laquelle vous cachez un graphique dépendrait de la fréquence à laquelle vous pensiez que les données qu’il représente pourraient changer.) La `SaveToCache` méthode nécessite `slidingExpiration` également un paramètre &#8212; si cela est réglé pour vrai, le compteur de délai d’attente est réinitialisé chaque fois que le graphique est consulté.</span><span class="sxs-lookup"><span data-stu-id="9cced-286">(The exact time you'd cache a chart would depend on how often you thought the data it represents might change.) The `SaveToCache` method also requires a `slidingExpiration` parameter &#8212; if this is set to true, the timeout counter is reset each time the chart is accessed.</span></span> <span data-ttu-id="9cced-287">Dans ce cas, cela signifie en effet que l’entrée de cache du graphique expire 2 minutes après la dernière fois que quelqu’un a accédé au graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-287">In this case, it in effect means that the chart's cache entry expires 2 minutes after the last time someone accessed the chart.</span></span> <span data-ttu-id="9cced-288">(L’alternative à l’expiration coulissante est l’expiration absolue, ce qui signifie que l’entrée du cache expirerait exactement 2 minutes après qu’elle a été mise dans le cache, peu importe la fréquence à laquelle elle avait été consultée.)</span><span class="sxs-lookup"><span data-stu-id="9cced-288">(The alternative to sliding expiration is absolute expiration, meaning that the cache entry would expire exactly 2 minutes after it was put into the cache, no matter how often it had been accessed.)</span></span>

    <span data-ttu-id="9cced-289">Enfin, le code `WriteFromCache` utilise la méthode pour aller chercher et rendre le graphique à partir du cache.</span><span class="sxs-lookup"><span data-stu-id="9cced-289">Finally, the code uses the `WriteFromCache` method to fetch and render the chart from the cache.</span></span> <span data-ttu-id="9cced-290">Notez que cette `if` méthode est en dehors du bloc qui vérifie le cache, car il obtiendra le graphique à partir du cache si le graphique était là pour commencer ou a dû être généré et enregistré dans le cache.</span><span class="sxs-lookup"><span data-stu-id="9cced-290">Note that this method is outside the `if` block that checks the cache, because it will get the chart from the cache whether the chart was there to begin with or had to be generated and saved in the cache.</span></span>

    <span data-ttu-id="9cced-291">Notez que dans `AddTitle` l’exemple, la méthode comprend un chrono.</span><span class="sxs-lookup"><span data-stu-id="9cced-291">Notice that in the example, the `AddTitle` method includes a timestamp.</span></span> <span data-ttu-id="9cced-292">(Il ajoute la date actuelle et `DateTime.Now` l’heure &#8212; &#8212; au titre.)</span><span class="sxs-lookup"><span data-stu-id="9cced-292">(It adds the current date and time &#8212; `DateTime.Now` &#8212; to the title.)</span></span>
5. <span data-ttu-id="9cced-293">Créez une nouvelle page nommée *ClearCache.cshtml* et remplacez son contenu par ce qui suit :</span><span class="sxs-lookup"><span data-stu-id="9cced-293">Create a new page named *ClearCache.cshtml* and replace its content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample14.cshtml)]

    <span data-ttu-id="9cced-294">Cette page `WebCache` utilise l’aide pour supprimer le graphique qui est mis en cache dans *ChartSaveToCache.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-294">This page uses the `WebCache` helper to remove the chart that's cached in *ChartSaveToCache.cshtml*.</span></span> <span data-ttu-id="9cced-295">Comme indiqué précédemment, vous n’avez normalement pas besoin d’avoir une page comme celle-ci.</span><span class="sxs-lookup"><span data-stu-id="9cced-295">As noted earlier, you don't normally have to have a page like this.</span></span> <span data-ttu-id="9cced-296">Vous le créez ici uniquement pour faciliter le test de mise en cache.</span><span class="sxs-lookup"><span data-stu-id="9cced-296">You're creating it here only to make it easier to test caching.</span></span>
6. <span data-ttu-id="9cced-297">Exécutez la page Web *ShowCachedChart.cshtml* dans un navigateur.</span><span class="sxs-lookup"><span data-stu-id="9cced-297">Run the *ShowCachedChart.cshtml* web page in a browser.</span></span> <span data-ttu-id="9cced-298">La page affiche l’image graphique en fonction du code contenu dans le fichier *ChartSaveToCache.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="9cced-298">The page displays the chart image based on the code contained in the *ChartSaveToCache.cshtml* file.</span></span> <span data-ttu-id="9cced-299">Prenez note de ce que dit l’ampoule dans le titre du graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-299">Take note of what the timestamp says in the chart title.</span></span> 

    ![Description: Image du tableau de base avec timestamp dans le titre du graphique](7-displaying-data-in-a-chart/_static/image13.jpg)
7. <span data-ttu-id="9cced-301">Fermez le navigateur.</span><span class="sxs-lookup"><span data-stu-id="9cced-301">Close the browser.</span></span>
8. <span data-ttu-id="9cced-302">Exécutez à nouveau le *ShowCachedChart.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="9cced-302">Run the *ShowCachedChart.cshtml* again.</span></span> <span data-ttu-id="9cced-303">Notez que le timestamp est le même qu’avant, ce qui indique que le graphique n’a pas été régénéré, mais a plutôt été lu à partir du cache.</span><span class="sxs-lookup"><span data-stu-id="9cced-303">Notice that the timestamp is the same as before, which indicates that the chart was not regenerated, but was instead read from the cache.</span></span>
9. <span data-ttu-id="9cced-304">Dans *ShowCachedChart.cshtml*, cliquez sur le lien **de cache Clear.**</span><span class="sxs-lookup"><span data-stu-id="9cced-304">In *ShowCachedChart.cshtml*, click the **Clear cache** link.</span></span> <span data-ttu-id="9cced-305">Cela vous emmène à *ClearCache.cshtml*, qui rapporte que le cache a été effacé.</span><span class="sxs-lookup"><span data-stu-id="9cced-305">This takes you to *ClearCache.cshtml*, which reports that the cache has been cleared.</span></span>
10. <span data-ttu-id="9cced-306">Cliquez sur le **lien Return to ShowCachedChart.cshtml,** ou re-run *ShowCachedChart.cshtml* de WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="9cced-306">Click the **Return to ShowCachedChart.cshtml** link, or re-run *ShowCachedChart.cshtml* from WebMatrix.</span></span> <span data-ttu-id="9cced-307">Notez que cette fois, le timestamp a changé, parce que le cache a été effacé.</span><span class="sxs-lookup"><span data-stu-id="9cced-307">Notice that this time the timestamp has changed, because the cache has been cleared.</span></span> <span data-ttu-id="9cced-308">Par conséquent, le code a dû régénérer le graphique et le remettre dans le cache.</span><span class="sxs-lookup"><span data-stu-id="9cced-308">Therefore, the code had to regenerate the chart and put it back into the cache.</span></span>

### <a name="saving-a-chart-as-an-image-file"></a><span data-ttu-id="9cced-309">Enregistrer un graphique comme fichier d’image</span><span class="sxs-lookup"><span data-stu-id="9cced-309">Saving a Chart as an Image File</span></span>

<span data-ttu-id="9cced-310">Vous pouvez également enregistrer un graphique sous forme de fichier d’image (par exemple, sous forme de fichier *.jpg)* sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="9cced-310">You can also save a chart as an image file (for example, as a *.jpg* file) on the server.</span></span> <span data-ttu-id="9cced-311">Vous pouvez ensuite utiliser le fichier d’image comme vous le feriez n’importe quelle image.</span><span class="sxs-lookup"><span data-stu-id="9cced-311">You can then use the image file the way you would any image.</span></span> <span data-ttu-id="9cced-312">L’avantage est que le fichier est stocké plutôt que enregistré dans un cache temporaire.</span><span class="sxs-lookup"><span data-stu-id="9cced-312">The advantage is the file is stored rather than saved to a temporary cache.</span></span> <span data-ttu-id="9cced-313">Vous pouvez enregistrer une nouvelle image graphique à des moments différents (par exemple, toutes les heures) et ensuite tenir un registre permanent des modifications qui se produisent au fil du temps.</span><span class="sxs-lookup"><span data-stu-id="9cced-313">You can save a new chart image at different times (for example, every hour) and then keep a permanent record of the changes that occur over time.</span></span> <span data-ttu-id="9cced-314">Notez que vous devez vous assurer que votre application Web a la permission d’enregistrer un fichier sur le dossier sur le serveur où vous souhaitez mettre le fichier d’image.</span><span class="sxs-lookup"><span data-stu-id="9cced-314">Note that you must make sure that your web application has permission to save a file to the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="9cced-315">À la racine de votre site Web, créez un dossier nommé \* \_ChartFiles\* s’il n’existe pas déjà.</span><span class="sxs-lookup"><span data-stu-id="9cced-315">At the root of your website, create a folder named *\_ChartFiles* if it does not already exist.</span></span>
2. <span data-ttu-id="9cced-316">À la racine de votre site Web, créez un nouveau fichier nommé *ChartSave.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-316">At the root of your website, create a new file named *ChartSave.cshtml*.</span></span>
3. <span data-ttu-id="9cced-317">Remplacer le contenu existant par les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9cced-317">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample15.cshtml)]

    <span data-ttu-id="9cced-318">Le code vérifie d’abord si le fichier *.jpg* existe en appelant la `File.Exists` méthode.</span><span class="sxs-lookup"><span data-stu-id="9cced-318">The code first checks to see whether the *.jpg* file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="9cced-319">Si le fichier n’existe pas, `Chart` le code crée un nouveau à partir d’un tableau.</span><span class="sxs-lookup"><span data-stu-id="9cced-319">If the file does not exist, the code creates a new `Chart` from an array.</span></span> <span data-ttu-id="9cced-320">Cette fois, le `Save` code appelle `path` la méthode et passe le paramètre pour spécifier le chemin de fichier et le nom de fichier de l’endroit où enregistrer le graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-320">This time, the code calls the `Save` method and passes the `path` parameter to specify the file path and file name of where to save the chart.</span></span> <span data-ttu-id="9cced-321">Dans le corps de `<img>` la page, un élément utilise le chemin pour pointer vers le fichier *.jpg* pour afficher.</span><span class="sxs-lookup"><span data-stu-id="9cced-321">In the body of the page, an `<img>` element uses the path to point to the *.jpg* file to display.</span></span>
4. <span data-ttu-id="9cced-322">Exécutez le fichier *ChartSave.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="9cced-322">Run the *ChartSave.cshtml* file.</span></span>
5. <span data-ttu-id="9cced-323">Retour sur WebMatrix.</span><span class="sxs-lookup"><span data-stu-id="9cced-323">Return to WebMatrix.</span></span> <span data-ttu-id="9cced-324">Notez qu’un fichier d’image nommé *chart01.jpg* a été enregistré dans le \* \_dossier ChartFiles.\*</span><span class="sxs-lookup"><span data-stu-id="9cced-324">Notice that an image file named *chart01.jpg* has been saved in the *\_ChartFiles* folder.</span></span>

### <a name="saving-a-chart-as-an-xml-file"></a><span data-ttu-id="9cced-325">Enregistrer un graphique comme fichier XML</span><span class="sxs-lookup"><span data-stu-id="9cced-325">Saving a Chart as an XML File</span></span>

<span data-ttu-id="9cced-326">Enfin, vous pouvez enregistrer un graphique en tant que fichier XML sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="9cced-326">Finally, you can save a chart as an XML file on the server.</span></span> <span data-ttu-id="9cced-327">Un avantage d’utiliser cette méthode sur la mise en cache du graphique ou l’enregistrement du graphique à un fichier est que vous pouvez modifier le XML avant d’afficher le graphique si vous le vouliez.</span><span class="sxs-lookup"><span data-stu-id="9cced-327">An advantage of using this method over caching the chart or saving the chart to a file is that you could modify the XML before displaying the chart if you wanted to.</span></span> <span data-ttu-id="9cced-328">Votre application doit avoir lu/écrire des autorisations pour le dossier sur le serveur où vous souhaitez mettre le fichier d’image.</span><span class="sxs-lookup"><span data-stu-id="9cced-328">Your application has to have read/write permissions for the folder on the server where you want to put the image file.</span></span>

1. <span data-ttu-id="9cced-329">À la racine de votre site Web, créez un nouveau fichier nommé *ChartSaveXml.cshtml*.</span><span class="sxs-lookup"><span data-stu-id="9cced-329">At the root of your website, create a new file named *ChartSaveXml.cshtml*.</span></span>
2. <span data-ttu-id="9cced-330">Remplacer le contenu existant par les éléments suivants :</span><span class="sxs-lookup"><span data-stu-id="9cced-330">Replace the existing content with the following:</span></span>

    [!code-cshtml[Main](7-displaying-data-in-a-chart/samples/sample16.cshtml)]

    <span data-ttu-id="9cced-331">Ce code est similaire au code que vous avez vu plus tôt pour stocker un graphique dans le cache, sauf qu’il utilise un fichier XML.</span><span class="sxs-lookup"><span data-stu-id="9cced-331">This code is similar to the code that you saw earlier for storing a chart in the cache, except that it uses an XML file.</span></span> <span data-ttu-id="9cced-332">Le code vérifie d’abord si le fichier `File.Exists` XML existe en appelant la méthode.</span><span class="sxs-lookup"><span data-stu-id="9cced-332">The code first checks to see whether the XML file exists by calling the `File.Exists` method.</span></span> <span data-ttu-id="9cced-333">Si le fichier existe, le `Chart` code crée un nouvel `themePath` objet et passe le nom de fichier comme paramètre.</span><span class="sxs-lookup"><span data-stu-id="9cced-333">If the file does exist, the code creates a new `Chart` object and passes the file name as the `themePath` parameter.</span></span> <span data-ttu-id="9cced-334">Cela crée le graphique basé sur tout ce qui est dans le fichier XML.</span><span class="sxs-lookup"><span data-stu-id="9cced-334">This creates the chart based on whatever's in the XML file.</span></span> <span data-ttu-id="9cced-335">Si le fichier XML n’existe pas déjà, le code `SaveXml` crée un graphique comme d’habitude, puis appelle pour l’enregistrer.</span><span class="sxs-lookup"><span data-stu-id="9cced-335">If the XML file doesn't already exist, the code creates a chart like normal and then calls `SaveXml` to save it.</span></span> <span data-ttu-id="9cced-336">Le graphique est rendu `Write` en utilisant la méthode, comme vous l’avez vu avant.</span><span class="sxs-lookup"><span data-stu-id="9cced-336">The chart is rendered using the `Write` method, as you've seen before.</span></span>

    <span data-ttu-id="9cced-337">Comme avec la page qui a montré la mise en cache, ce code inclut un timestamp dans le titre du graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-337">As with the page that showed caching, this code includes a timestamp in the chart title.</span></span>
3. <span data-ttu-id="9cced-338">Créez une nouvelle page nommée *ChartDisplayXMLChart.cshtml* et ajoutez la marque suivante :</span><span class="sxs-lookup"><span data-stu-id="9cced-338">Create a new page named *ChartDisplayXMLChart.cshtml* and add the following markup to it:</span></span> 

    [!code-html[Main](7-displaying-data-in-a-chart/samples/sample17.html)]
4. <span data-ttu-id="9cced-339">Exécutez la page *ChartDisplayXMLChart.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="9cced-339">Run the *ChartDisplayXMLChart.cshtml* page.</span></span> <span data-ttu-id="9cced-340">Le graphique est affiché.</span><span class="sxs-lookup"><span data-stu-id="9cced-340">The chart is displayed.</span></span> <span data-ttu-id="9cced-341">Prenez note de l’éd du temps dans le titre du graphique.</span><span class="sxs-lookup"><span data-stu-id="9cced-341">Take note of the timestamp in the chart's title.</span></span>
5. <span data-ttu-id="9cced-342">Fermez le navigateur.</span><span class="sxs-lookup"><span data-stu-id="9cced-342">Close the browser.</span></span>
6. <span data-ttu-id="9cced-343">Dans WebMatrix, cliquez à droite sur le **Refresh** \* \_dossier ChartFiles,\* cliquez sur Refresh, puis ouvrez le dossier.</span><span class="sxs-lookup"><span data-stu-id="9cced-343">In WebMatrix, right-click the *\_ChartFiles* folder, click **Refresh**, and then open the folder.</span></span> <span data-ttu-id="9cced-344">Le fichier *XMLChart.xml* dans ce dossier `Chart` a été créé par l’aide.</span><span class="sxs-lookup"><span data-stu-id="9cced-344">The *XMLChart.xml* file in this folder was created by the `Chart` helper.</span></span> 

    ![Description: Le dossier _ChartFiles montrant le fichier XMLChart.xml créé par l’aide graphique.](7-displaying-data-in-a-chart/_static/image14.jpg)
7. <span data-ttu-id="9cced-346">Exécutez à nouveau la page *ChartDisplayXMLChart.cshtml.*</span><span class="sxs-lookup"><span data-stu-id="9cced-346">Run the *ChartDisplayXMLChart.cshtml* page again.</span></span> <span data-ttu-id="9cced-347">Le graphique affiche le même timetamp que la première fois que vous avez couru la page.</span><span class="sxs-lookup"><span data-stu-id="9cced-347">The chart shows the same timestamp as the first time you ran the page.</span></span> <span data-ttu-id="9cced-348">C’est parce que le graphique est généré à partir de la XML que vous avez enregistré plus tôt.</span><span class="sxs-lookup"><span data-stu-id="9cced-348">That's because the chart is being generated from the XML you saved earlier.</span></span>
8. <span data-ttu-id="9cced-349">Dans WebMatrix, \* \_\* ouvrez le dossier ChartFiles et supprimez le fichier *XMLChart.xml.*</span><span class="sxs-lookup"><span data-stu-id="9cced-349">In WebMatrix, open the *\_ChartFiles* folder and delete the *XMLChart.xml* file.</span></span>
9. <span data-ttu-id="9cced-350">Exécutez la page *ChartDisplayXMLChart.cshtml* une fois de plus.</span><span class="sxs-lookup"><span data-stu-id="9cced-350">Run the *ChartDisplayXMLChart.cshtml* page once more.</span></span> <span data-ttu-id="9cced-351">Cette fois, le timetamp est `Chart` mis à jour, parce que l’aide a dû recréer le fichier XML.</span><span class="sxs-lookup"><span data-stu-id="9cced-351">This time, the timestamp is updated, because the `Chart` helper had to recreate the XML file.</span></span> <span data-ttu-id="9cced-352">Si vous le \* \_\* souhaitez, vérifiez le dossier ChartFiles et remarquez que le fichier XML est de retour.</span><span class="sxs-lookup"><span data-stu-id="9cced-352">If you want, check the *\_ChartFiles* folder and notice that the XML file is back.</span></span>

<a id="Additional_Resources"></a>
## <a name="additional-resources"></a><span data-ttu-id="9cced-353">Ressources supplémentaires</span><span class="sxs-lookup"><span data-stu-id="9cced-353">Additional Resources</span></span>

- [<span data-ttu-id="9cced-354">Introduction à travailler avec une base de données dans ASP.NET sites Web</span><span class="sxs-lookup"><span data-stu-id="9cced-354">Introduction to Working with a Database in ASP.NET Web Pages Sites</span></span>](https://go.microsoft.com/fwlink/?LinkId=202893)
- [<span data-ttu-id="9cced-355">Utilisation de caching dans ASP.NET sites de pages Web pour améliorer les performances</span><span class="sxs-lookup"><span data-stu-id="9cced-355">Using Caching in ASP.NET Web Pages Sites to Improve Performance</span></span>](https://go.microsoft.com/fwlink/?LinkId=202903)
- <span data-ttu-id="9cced-356">[Catégorie graphique](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET référence de l’API pages Web sur MSDN)</span><span class="sxs-lookup"><span data-stu-id="9cced-356">[Chart Class](https://msdn.microsoft.com/library/system.web.helpers.chart(v=vs.99)) (ASP.NET Web Pages API reference on MSDN)</span></span>
